# Sorted v2 PR List (MVP)

Social-media post approval tool. Greenfield. Org `srtdio`, repo `app`. Author: Shubham Gune.
Regenerated 23 May 2026 after the MVP stripdown. Replaces the old 67-PR list (publishing, scheduling, plan, insights, approvals-table, share-tokens all dead).

## 0. Project identifiers and DANGER

| Env | Project ID | Status | Touch from v2 PRs? |
| --- | --- | --- | --- |
| v2 | movnexawfhsyuluspxoc | Build target | YES |
| v1 | ozptjplxbyswclolbxyn | LIVE PRODUCTION | NO. Only the cutover PR, only on cutover day. |

Both projects are Mumbai. Region is NOT a disambiguator. Project ID is the only safe identifier.
Every Claude Code prompt that touches Supabase MUST pass `project_id="movnexawfhsyuluspxoc"` explicitly.
The v1 project ID appears only in the ETL script and the cutover PR.

## 1. How to read

- PRs are numbered. Numbers are labels, not a fixed order. Order will change.
- "Depends on" lists the PR numbers that must merge first. "none" means standalone, can run anytime in parallel.
- No status column, no batch gates. Statuses drift; this list does not track them.
- Grouped only by the three locked build phases (PRD section 24): Backend, ETL, Frontend.
  No interleaving across phases. No PR depends on a later phase.
- Approval is a post stage transition, not a feature. No publishing, scheduling, plan, or insights anywhere.

## 2. Phase 1: Backend

| # | Title | Depends on |
| --- | --- | --- |
| 1 | Regenerate baseline migration from the stripped live DB: all 30 base tables + 3 partitioned parents + 9 partitions, RLS, triggers, indexes, public.users + auto-create trigger, all *_by FK rules. Replaces the old baseline + rename_stages migrations. Schema already applied. Do NOT execute. | none |
| 2 | Regenerate supabase.generated.ts and Zod schemas from the stripped live DB. Drop all dead-table types. Drift gate green. | 1 |
| 3 | Leftover-trace cleanup migration: drop dead CHECK values (comments/inbox plan_cell + plan_period; chat_channels plan; intent_ledger share_token), drop workspace_onboarding.linkedin_connected_at + first_schedule_at, add webhook_events.source value agora. SQL stated in chat and approved first. | 1 |
| 4 | RLS CI test suite: cross-tenant isolation tests for every tenant-scoped table | 1 |
| 5 | SECURITY DEFINER procs consolidated: stage_transition, post_version_create, annotation_create, member_invite, member_accept, brief_create, brief_close. INSERT/UPDATE/DELETE revoked from authenticated on sensitive tables. | 1, 4 |
| 6 | Auth: magic-link signup, login, password, JWT 15 min + refresh | 1, 4 |
| 7 | session_devices fingerprint capture + RLS gate on every auth request | 6 |
| 8 | Workspace creation + member invite + role assignment (owner/admin/agency/client) + workspace_role_permissions defaults + public.users profile editing (display_name, designation, avatar upload to user-avatars bucket) + workspace_create client-callable SECURITY DEFINER proc (EXECUTE granted to authenticated, revoked from public) + regenerate supabase.generated.ts, drift gate green. Schema already applied. Do NOT execute. | 6, 5 |
| 9 | Idempotency-Key middleware on all mutating endpoints | none |
| 10 | Audit log write helper, indexed on trace_id (table already exists) | none |
| 11 | Posts domain: stage state machine (draft to review/parked, review to approved/rejected/parked, approved to parked/rejected, parked and rejected revive to review, approved not terminal; matrix CHECK already in DB) + post create, version, annotate APIs via SECURITY DEFINER. post_versions and post_annotations immutable, no deleted_at. + E2E on every transition. | 5 |
| 12 | idempotency_keys table: service-role-only infra table (RLS enabled, no policies; service_role bypasses RLS and is the sole writer), unique scope index (key, workspace_id, user_id) NULLS NOT DISTINCT, expires_at index + regenerate supabase.generated.ts and Zod schemas, drift gate green. Schema already applied. Do NOT execute. | 1 |
| 13 | Comments primitive backend: entity-anchored, reactions, edit window, soft-delete, decision flag, Supabase Realtime channel + E2E | 5 |
| 14 | Briefs backend: client-only create, open/closed, comment + close, no edits after creation, derived linked-post count + E2E | 5 |
| 15 | Assets backend: upload pipeline (MIME allowlist, EXIF strip, SVG sanitize, virus scan), R2 per-workspace bucket, asset_versions chain, asset_attachments bind to asset_version_id (NO ACTION) | none |
| 16 | Inbox backend: inbox_entries writes for all MVP event types, scope routing, snooze, partition-ahead cron | 5 |
| 17 | Agora setup: channel ID convention (dm/group only), 15-min token mint endpoint JWT-aligned, per-channel ACL at creation | 1 |
| 18 | Chat mirror cron (Agora REST to chat_messages, per-channel cursor) + reconciliation cron 04:00 UTC (>5% P3, >20% P2) | 17, 3 |
| 19 | Groups backend: create, naming rules, group_members, Agora ACL mirror via REST | 8 |
| 20 | Email backend: Resend integration, email_threads, delivery_attempts, bounce/complaint webhooks, 9am-9pm workspace-TZ bundler, Message-ID chains (brief if linked, else post) | 10 |
| 21 | Push backend: FCM + APNs + Web Push, urgent-only, gated 9am-9pm workspace TZ, per-user opt-in | 20 |
| 22 | Search prep: tsvector indexes on comments, posts, briefs, assets | 1 |
| 23 | Cockpit backend: passkey auth, platform_operators, audit_log wiring, intent_ledger, pending_flows, allowlisted procs, impersonation (capability-scoped, 30-min cap) | 5, 10 |
| 24 | Feature flags backend: killswitch / rollout / experiment / tier-gated, global + per-workspace | 1 |
| 25 | Stripe billing backend: per-workspace tiers, 14-day trial, nudge cadence (day 7/10/12/13/14), lifecycle states (trial/active/grace/soft_pause/full_pause/soft_delete), reconciliation cron | 8 |
| 26 | Trash backend: soft-delete + restore for assets (30 days) and workspaces (7 days), cleanup cron | 15 |
| 27 | Alerts and monitoring: webhook signature failures, RLS CI, capability audit CI, mirror cron failures, Agora reconciliation drift | 4, 10 |
| - | Repo hygiene: stop tracking .env.production (public repo). git rm --cached only, file stays on disk; drop the !.env.production negation in .gitignore so the .env.* rule covers it and no production env file can be committed (a real secret could land there later). Tracking-only, no schema. | none |
| - | Scaffold @srtdio/storage package (scaffold) | none |
| - | Move R2 client, buildR2Key, assetBucketName, sha256, mime into @srtdio/storage (runtime-neutral; R2 client receives a traced-fetch via constructor) and repoint app worker imports | 15 |
| - | Chat token Worker: mints 24-hour Agora Chat user tokens for authenticated workspace members (username derived from the Supabase user id via toAgoraUsername, since Agora rejects UUID-shaped names; reversible via fromAgoraUsername), ensures the Agora Chat user exists via REST (idempotent), reuses asset-read's ES256 verify + service-role membership read. Mint only; renewal via SDK onTokenWillExpire, revocation via channel ACL. | 15, 17 |
| - | Chat webhook-mirror Worker: receives Agora Chat post-delivery webhooks, verifies the md5(callId+secret+timestamp) signature before processing (401 + no proc call on failure), and records each event to the DB via the service-role-only chat_webhook_ingest proc (bare service-role supabase-js client, no minted member JWT). Parses source_event_id/event_type and, for message events, channel_id + message id + agora_event_id + body + mentions + attachment asset ids + created_at, reverse-mapping the sender via fromAgoraUsername. Idempotency is the proc + DB unique indexes (no KV dedupe); success/skipped/failure all ack 200 (failure logged loudly, raw event captured for the reconciliation cron), only signature failure or an unreachable DB returns non-2xx. Compliance mirror only, never on any read path. Retroactive migration records the live-applied proc + grants (do NOT execute). | 15, 17 |
| - | Legitimize the out-of-band folders schema: record the live-applied folders table (workspace-scoped self-referential tree, cycle-guard trigger, folders_select_member RLS policy) and assets.folder_id FK (SET NULL) + index as a retroactive migration, regenerate supabase.generated.ts, drift gate green. folder_path retained pending a later reconciliation decision. Flag only (do not fix): folders has SELECT/INSERT/UPDATE/DELETE revoked from authenticated and service_role, so its SELECT policy is unreachable for the app role. Schema already applied. Do NOT execute. | 1, 15 |
| - | Folders RLS hardening: record the live-applied folders grants/policy fix (rebind folders_select_member to the authenticated role, GRANT SELECT to authenticated, GRANT SELECT/INSERT/UPDATE/DELETE to service_role) as a retroactive migration, and extend the RLS CI suite to cover folders with the same anon-deny + cross-tenant isolation matrix as assets. Closes the unreachable-policy / no-service-role-write-path flags from the folders legitimization PR. Schema already applied. Do NOT execute. Must merge AFTER the folders legitimization PR. | 4 |
| - | Folder write API on the asset-upload Worker: add POST /folders (create, any active member), /folders/rename + /folders/delete (owner/admin/agency only), /folders/move (batch asset re-file, any active member), each behind the existing ES256 verifyCaller + service-role membership gate, with a 1..80 folder-name validator, a folder_name_taken (409) code on the folders_unique_name unique violation, verified-uid actor and trace propagation mirroring POST /links, repository methods (createFolder, renameFolder, softDeleteFolderWithDetach reparent-then-detach-then-tombstone, moveAssetsToFolder) + success-path folder audit rows. Backend only, no schema, no SQL, no frontend. | 15 |
| - | Group + channel procs (A2a): record the live-applied group_create, group_rename, group_member_add, group_member_remove, group_leave, dm_channel_ensure SECURITY DEFINER procs (search_path='', EXECUTE to authenticated only) as a retroactive migration, regenerate supabase.generated.ts, drift gate green. Gating: group_create / dm_channel_ensure require an active workspace member; group_rename / group_member_add / group_member_remove require the group creator or a workspace owner/admin; group_leave is self only. Schema already applied. Do NOT execute. | 8, 17 |
| - | Chat data-layer proc wrappers: add six typed, Result-returning @srtdio/rpc wrappers (groupCreate, groupRename, groupMemberAdd, groupMemberRemove, groupLeave, dmChannelEnsure) over the group/channel SECURITY DEFINER procs, args typed verbatim from the generated types with p_trace_id supplied by the caller, no-throw on domain error. Reads live elsewhere (out of scope). Edits packages/rpc alone so dependent chat PRs can import it. | 8, 17 |
| - | Agora-sync Worker (A2b): Realtime consumer (bare service-role supabase-js, stateless, idempotent, one uuid_v7 trace per event) that mirrors group/channel state into Agora Chat via REST (app token from APP_ID + APP_CERTIFICATE). chat_channels INSERT creates the Agora group with the mapped owner + member usernames (DM channels have no Agora object) and records the returned group id via the service-role-only chat_channel_mark_synced proc; group_members INSERT/DELETE add/remove the member; groups UPDATE renames the group. Member events for a not-yet-synced group are a deferred no-op (Realtime redelivery / reconciliation cron is the backstop), and adding an existing member, removing an absent one, and re-handling an already-synced channel are safe no-ops; a failed Agora call is logged loudly and swallowed per event so the subscription never tears down. Mirror tweak: the chat-webhook-mirror resolves a group message's agora_group_id to our channel_id (null when unsynced) before chat_webhook_ingest, keeping the DM reconstruction. Retroactive migration records the live-applied chat_channels.agora_group_id column + partial unique index + service_role SELECT grants + chat_channel_mark_synced proc (do NOT execute); regenerate supabase.generated.ts, drift gate green. Schema already applied. Do NOT execute. | 17, 19 |
| - | PCS write procs (B-schema): record the live-applied post_update, post_caption_update, comment_edit, comment_soft_delete, gallery_set + _post_snapshot helper SECURITY DEFINER procs (search_path=public) as a retroactive migration, regenerate supabase.generated.ts, drift gate green. post_update / post_caption_update / gallery_set require an active workspace member with the post.edit capability (caption + gallery also auto-version via post_version_create); comment_edit / comment_soft_delete are author-only on a live comment. No table or column changes. _post_snapshot is internal (EXECUTE revoked from public + authenticated); the five client procs grant EXECUTE to authenticated only. RPC bindings, Zod validators, and tests are a later PR. Schema already applied. Do NOT execute. | 5, 11, 13 |
| - | PCS write proc data-layer (B-bind): wire the five B-schema PCS write procs into the app. Add typed, Result-returning @srtdio/rpc bindings (commentEdit, commentSoftDelete, gallerySet, postCaptionUpdate, postUpdate) with Args/Returns taken verbatim from the generated Functions types and p_trace_id carried like the existing bindings; plus domain wrappers with proc-arg Zod in @srtdio/posts (postUpdate, postCaptionUpdate, gallerySet) and @srtdio/comments (editComment, deleteComment). postUpdate is key-presence: only caller-provided keys reach p_payload (explicit null on ownerUserId/targetDate clears them, omitted keys are left untouched). Each wrapper Zod-validates input first, returns the Result type, never throws on expected failure, never returns null, never accepts an actor id (actor is auth.uid() server-side). Success + boundary tests (invalid-input and forbidden-role mapped to a Result error, not a throw) wired into the existing posts and comments suites. No schema, no SQL, no type regen. | 5, 11, 13 |
| - | Comment + stage Activity side-effects (B-activity): record the live-applied CREATE OR REPLACE of comment_create and stage_transition as a retroactive migration (signatures unchanged, grants preserved by REPLACE, no type regen). comment_create now guards one-level threading (a reply to an already-threaded comment is invalid_payload), lands attachment_asset_ids in Assets as entity_type='comment' asset_attachments rows pinning each asset's current_version_id (an asset with no current version or in another workspace is invalid_payload), and emits inline Activity inbox_entries: a 'comment' row for other active members (client role excluded while the post stage is draft) and an urgent 'mention' row for mentioned active members. stage_transition emits a 'stage_change' row for every active member except the actor. Regression cases mirror the multi-role proc suite. Schema already applied. Do NOT execute. | 5, 11, 13 |
| 51 | Posts stage_entered_at column and stage_transition stamp | none |
| - | Schema: add users.timezone for catch-up scheduling: record the live-applied public.users.timezone (text, nullable; IANA tz, null falls back to workspaces.timezone for catch-up email scheduling, app-validated, no DB CHECK) as a retroactive migration and regenerate supabase.generated.ts + the users Zod schema, drift gate green. Foundation only; the catch-up Worker and deep-link frontend are later PRs. Schema already applied. Do NOT execute. | 1 |
| - | Schema: record the live-applied legacy_author_name (text, nullable) column added to posts, briefs, and post_versions for ETL provenance, hand-editing supabase.generated.ts in alphabetical position across Row/Insert/Update for each table (regen unavailable here; CI Schema Drift validates the edit against the live DB). Schema already applied. Do NOT execute. | 1 |
| - | Schema: relax legacy author columns to nullable: record the live-applied DROP NOT NULL on posts.created_by, posts.owner_user_id, briefs.created_by, post_versions.created_by, assets.uploaded_by (their FKs are already ON DELETE SET NULL, so NOT NULL was a defect) as a retroactive migration and regenerate supabase.generated.ts (drift gate green; the five columns become nullable across Row/Insert/Update). Schema already applied. Do NOT execute. | 1 |
| - | Schema: relax remaining ON DELETE SET NULL columns to nullable: record the live-applied DROP NOT NULL on asset_versions.uploaded_by, asset_attachments.attached_by, groups.created_by, cockpit_procedure_allowlist.added_by (their FKs are already ON DELETE SET NULL, so NOT NULL was a defect) as a retroactive migration and regenerate supabase.generated.ts (drift gate green; the four columns become nullable across Row/Insert/Update). The one consumer that assumed groups.created_by non-null (chat-agora-sync getGroup) is widened to string|null and skips a creator-less group, mirroring the recipients.ts null handling. Schema already applied. Do NOT execute. | 1 |
| - | DB trigger: backfill assets.display_name from parent title on attach: record the live-applied asset_attachment_set_display_name() function + AFTER INSERT trigger on asset_attachments (AFTER INSERT, SECURITY DEFINER, search_path=public) as a retroactive migration. On attach it fills a BLANK assets.display_name (NULL or whitespace only) from the attachment's parent-context title (post -> title; brief -> title + ' (brief)'; comment -> the comment's owning post/brief title + ' (comment)'), clamped to 200 chars, never overwriting a manual rename or ETL-migrated name; chat_message attachments are skipped. A trigger function returns trigger and is not in the generated type surface, so no type regen and drift stays green. Regression test in the rpc suite inserts attachment rows via the admin client and asserts display_name per entity_type plus the blank-only guard. Schema already applied. Do NOT execute. | 1, 15 |

| - | Invite edge functions asymmetric-key auth fix: the platform gateway JWT check rejected valid caller tokens under the new asymmetric Supabase key system before invite-send / invite-accept ever ran. Per Supabase guidance, disable the gateway check for both functions (config.toml [functions.invite-send] / [functions.invite-accept] verify_jwt = false) AND verify the caller inside each function via callerClient.auth.getUser() before any side effect: invite-send gates BEFORE provisioning (createUser) so no unauthenticated request can reach it, invite-accept gates BEFORE member_accept; on error or no user both console.error(traceId, message) and return { error: 'unauthorized', error_detail, trace_id } with HTTP 401. Deploy workflow relies on config.toml (no per-function verify-jwt flag), so unchanged. Edge-only, no schema, no frontend. | 5, 8 |
| - | Invite edge functions CORS allow-headers fix: the browser blocked invite-send / invite-accept at preflight because each function's corsHeaders Access-Control-Allow-Headers omitted apikey and x-client-info, the two headers supabase-js attaches to every functions.invoke call, so the OPTIONS preflight failed and the POST was never sent. Both functions now allow authorization, apikey, x-client-info, content-type, x-trace-id, x-device-fingerprint, and strip a single trailing '/' from the APP_BASE_URL value before using it as Access-Control-Allow-Origin so it matches the browser Origin exactly (origin still sourced from APP_BASE_URL, no hardcoded domain). Auth gate, provisioning, RPCs, magiclink, Resend, delivery_attempts, trace id, and response shapes unchanged. Edge-only, no schema, no frontend. | 5, 8 |
| - | Invite email link + template fix: the emailed magiclink redirected to /invite/set-password, which has no route, so the logged-in invitee fell through to /pipeline and saw "No workspace yet". invite-send now points generateLink redirectTo at the existing /invite/accept?invite=<id> page (reads ?invite, shows an Accept button, calls invite-accept), and replaces the bare inline HTML with a self-contained responsive email (centered ~520px card carrying its own background + high-contrast text, legible on light or dark clients, accent button on the action link, plaintext fallback URL below it). escapeHtml still applied to workspaceName/role/url; both { html, text } returned and the Resend payload now sends text alongside html. Auth gate, provisioning, member_invite, delivery_attempts, CORS, trace id, response shapes, and subject unchanged. Edge-only, no schema, no frontend. | 5, 8 |
| - | Invite email visual redesign: replace the invite-send inline HTML with a branded, table-based responsive card (Sorted wordmark + logo chip, deep-violet #1e1b2e canvas, white 560px card, accent #5a3fe0 "Accept invitation" button, eyebrow + headline + body + footer) using inline CSS only and no style block; update the plaintext body to match with the full raw accept URL on its own line, and set the subject to "You're invited to {workspaceName} on Sorted". escapeHtml still applied to workspaceName/role/url at the three substitution points; the Resend payload still sends both html and text. Visual-only: auth gate, provisioning, member_invite, delivery_attempts, CORS, trace id, response shapes, magiclink, acceptUrl, and the email-body builder signature unchanged. Edge-only, no schema, no frontend. | 5, 8 |
| - | Onboarding profile setup screen (frontend): a brand-new user (just past an invite) lands on a full-screen, no-chrome profile form they cannot skip. AvatarCropper ports the approved prototype (square 260px FRAME, 512px PNG export, one-directional zoom from baseScale to baseScale*3) with choose-from-device, slider + -/+ buttons, single-finger drag and two-finger pinch via pointer events, and optional desktop wheel zoom; the image always covers the circular crop guide. The form collects full name (required) + designation (optional) + an "email me the daily catch-up" toggle (default on), shows live comment / notification / workspace-chip previews via the Avatar component, exports the cropped PNG, uploads it to the avatar-upload Worker (uploadAvatarFile mirrors asset-upload: multipart, Bearer, fetchWithTrace) and saves the returned URL plus the fields via the user_profile_update proc (submitProfileSetup orchestration, unit-tested without canvas). AppLayout gains a top-of-render gate on a new useCurrentProfile hook: while loading it shows the shell loading state, when profile_completed_at is null it renders the form and returns (escaping chrome), and it fails open on a load error so a transient fetch never traps the user. Adds VITE_AVATAR_UPLOAD_URL (optional). Frontend only, no schema, no worker, no new deps. | none |
| - | Onboarding profile write path (backend): record the live-applied public.users.profile_completed_at (timestamptz, nullable, onboarding gate stamped once an avatar exists) and email_opt_in (boolean, not null default true, catch-up email opt-out) columns plus the user_profile_update(p_display_name, p_designation, p_avatar_url, p_email_opt_in, p_trace_id) SECURITY DEFINER proc (search_path='', EXECUTE to authenticated only) as a retroactive migration; the proc is the only write path to a user's own profile, acts on auth.uid(), raises invalid_payload on a null/blank display_name, coalesces designation/avatar_url/email_opt_in, stamps profile_completed_at once an avatar exists, and writes a success audit row. Regenerate supabase.generated.ts via the Supabase MCP + add the two users Zod fields, add the typed Result-returning @srtdio/rpc userProfileUpdate wrapper (args verbatim from the generated types, p_trace_id carried, no-throw on domain error) with success + invalid_payload tests, sync docs, drift gate green. Backend only, no UI, no worker. Schema already applied to movnexawfhsyuluspxoc. Do NOT execute. | none |
| - | Record the live-applied user_profile_update designation nullif hot fix as a do-not-execute migration: a blank designation was being written as '' and tripping users_designation_check, so the CREATE OR REPLACE coalesces a trimmed-blank designation to "leave unchanged" (designation = coalesce(nullif(trim(p_designation), ''), designation)); every other line, the signature, and the grants match the original proc. Record-only, do-not-execute; signature unchanged so no types regen, no zod, no rpc, no app change. | none |
| - | Record comments.legacy_author_name + legacy_author_email as a do-not-execute migration: both columns were applied out-of-band on the v2 project and never recorded in the generated types, so the schema-drift gate was red repo-wide (pre-existing, not from the backfill PR). Regenerate supabase.generated.ts via the Supabase MCP (only diff: legacy_author_name and legacy_author_email gained in the comments Row/Insert/Update blocks), add the parity migration 20260621180000_comments_legacy_author_columns.sql (ALTER TABLE comments ADD legacy_author_name text 1..120 + legacy_author_email text 3..320, both nullable, with the two char_length CHECK constraints), and note both columns in docs/schema.md. legacy_author_name freezes the original author name for migrated v1 comments; legacy_author_email reclaims authorship when that person later joins with the same email. Recording only, comments columns only, no DB execution. Schema already applied to movnexawfhsyuluspxoc. Do NOT execute. | none |
| - | Record the member_accept reclaim change as a do-not-execute migration: member_accept now reclaims a joining member's migrated v1 comments by email when they accept their invite (after the membership activation + audit row, it lowercases the invitee's auth.users email and, for this workspace's comments whose legacy_author_name is set and legacy_author_email matches, re-attributes author_user_id to the new account and clears both frozen legacy labels). CREATE OR REPLACE body-only change; the (p_invite_id uuid, p_trace_id uuid) RETURNS uuid signature, SECURITY DEFINER, search_path='', and grants are unchanged, so generated types do NOT move. Adds migration 20260621190000_member_accept_reclaim_legacy_comments.sql only. Recording only, no DB execution. Schema already applied to movnexawfhsyuluspxoc. Do NOT execute. | 5 |

## 3. Phase 2: ETL

| # | Title | Depends on |
| --- | --- | --- |
| 28 | ETL v1 to v2 single-shot: dev-seed mode (PII scrubbed) + cutover mode. Brings over workspaces, posts, briefs, assets, comments. Does NOT bring over users (fresh signup post-cutover). Legacy *_by columns set NULL, displayed as ex-member. Owner set to agency operator at ETL time. The v1 project ID appears here. | 1, 5 |
| 29 | Cutover run: interceptor on v1, ETL cutover mode, checksum validator, rollback rehearsal. Post-cutover the agency invites each client fresh via the invite flow. The v1 project ID appears here. | 28, and a working backend (Phase 1 complete) |
| 30 | ETL Run (manual): workflow_dispatch-only GitHub Actions workflow (.github/workflows/etl-run.yml) to run the @srtdio/etl migration. Inputs: mode (dev-seed/cutover), dry_run, operator_email, operator_display_name, workspace_name, confirm_cutover. Guard step fails fast on cutover without confirm_cutover. Never fires on push/PR. Touches no package source. | 28 |
| - | ETL dev-seed Run (manual): workflow_dispatch-only GitHub Actions workflow (.github/workflows/etl-dev-seed.yml) that runs @srtdio/etl in hardcoded --mode=dev-seed, never --confirm-cutover, no cutover path. Mirrors etl-ci.yml setup (checkout@v4, pnpm/action-setup@v4, setup-node@v4 node 22 + pnpm cache, frozen-lockfile install). Never fires on push/PR (wipes and reloads the dev-seed workspace content in v2). Secrets only via ${{ secrets.* }} (TARGET via SUPABASE_DB_URL, ref via SUPABASE_PROJECT_ID). Touches no package source. | 28 |
| - | ETL post stage mapping fix: map v1 ready to v2 draft (the locked decision), not approved. mapPostStage in packages/etl/src/transform.ts drops ready from the approved group and adds an explicit ready -> draft case; published/scheduled still map to approved, awaiting_* -> review, rejected -> rejected, parked -> parked, is_draft and the default still -> draft. Doc comment and unit tests updated. Code-only, no schema change, nothing touches v1. | 28 |
| - | ETL legacy author-name attribution: carry each v1 author's display name onto the nullable legacy_author_name column on posts, briefs, and post_versions while created_by/owner_user_id stay = operatorUserId. source.ts resolves the name read-only from public.profiles (by owner_profile_id, by id::text, and by lower(email)) with email/uuid guards so the result is a real display_name or NULL, never a raw email or uuid (posts via owner_profile_id/created_by, briefs via requests.created_by, post_versions via pv.edited_by); the V1Post/V1Request/V1PostVersion row types gain legacy_author_name. A pure transform.ts helper redactAuthorName(name, scrub) mirrors the edited_by snapshot redaction exactly (dev-seed -> 'Member', cutover passes through, null stays null and renders as "(ex-member)"), reused by load.ts on all three inserts under the SAME scrub flag. Unresolvable authors stay null. Unit tests cover the redactAuthorName boundaries. Code-only, no schema change, source layer stays SELECT-only, nothing writes to v1. | 28 |
| - | ETL dev-seed asset safety: in dev-seed mode the asset migration creates asset/asset_version/asset_attachment ROWS but fetches NO image bytes and uploads NOTHING to R2 (cutover is unchanged: real fetch + sniff + putObject). processFile gains a dev-seed branch right after the byPath dedup early-return and before any fetch: mime/kind come from the filename extension (extensionMime, now exported from transform.ts), the version is gated by the same isAllowedMime allowlist, sha256 is a deterministic computeSha256 of the URL string (stable re-runs, same-URL bySha dedup), size_bytes is a synthetic positive 1, and r2_key is built as usual, so the synthesized non-link version still satisfies asset_versions_kind_shape (r2_key + sha256 + size_bytes + mime_type present, external_url null) while carrying the real image/video kind. PlanDeps gains a required devSeed flag, set from the mode in loadAssets; no deps.fetchBytes or storage.putObject runs on the dev-seed branch. Closes the locked "dev-seed = no image bytes, PII-safe" rule and the orphaned-R2-object leak. New tests drive buildAssetPlan with throw-on-use fetch + storage stubs proving zero network/R2 I/O and same-URL dedup. Code-only, no schema change, cutover path byte-for-byte unchanged, nothing touches v1. | 28 |
| - | ETL dev-seed asset cleanup: pure refactor + test relocation, no behavior change. The isDevSeed discriminator, previously duplicated identically in load.ts and assets.ts, is consolidated into one exported helper in config.ts and imported by both (no import cycle: config.ts imports neither). The dev-seed asset tests are relocated into the single tests/etl/assets.test.ts (reusing its existing CTX and fileItem helper, with local throw-on-use fetch/storage stubs) and the duplicate packages/etl/tests/assets.test.ts is deleted, leaving one assets test file. isDevSeed keeps its exact logic and every existing test keeps its assertions. Code-only, no schema change, nothing touches v1. | 28 |
| - | ETL config trim env values on read: the local get(name) helper inside loadConfig (packages/etl/src/config.ts) tested value.trim() === '' for emptiness but returned the UNtrimmed value, so a secret pasted with surrounding whitespace or a trailing newline (common from copy-paste) passed the empty check and was returned with the newline, failing UUID_RE for OPERATOR_USER_ID and corrupting the DB URLs. get now trims once (raw === undefined ? '' : raw.trim()) and returns the trimmed value, keeping the same missing-variable behaviour; workspaceTimezone already trimmed and is unchanged. No validation rule changed. A regression test in tests/config.test.ts builds a complete valid env, sets OPERATOR_USER_ID to a valid uuid wrapped in surrounding whitespace + a trailing newline and asserts loadConfig does not throw and operatorUserId is the clean 36-char uuid, plus that a required string var (TARGET_WORKSPACE_NAME) comes back trimmed. Code-only, no schema change, nothing touches v1. | 28 |
| - | ETL ambiguous column fix: the author-attribution LEFT JOIN public.profiles added to fetchPostsBatch and fetchRequestsBatch in packages/etl/src/source.ts left their base-table SELECT columns unqualified, and public.profiles shares names (title, status, created_at, updated_at, ...), so Postgres raised "column reference is ambiguous". Every base column in the two SELECT lists is qualified with the posts alias p (p.post_id, p.title, p.caption, p.format, p.is_draft, p.stage, p.brief_id, p.target_date, p.created_at, p.updated_at) and the requests alias r (r.title, r.description, r.content_type, r.status, r.target_date, r.drive_link, r.images). The COALESCE legacy_author_name expressions, JOINs, WHERE, ORDER BY, LIMIT/OFFSET, and params are untouched; fetchVersionsForPosts and the comment queries are untouched. Regression check is the dev-seed ETL run completing without an ambiguity error. Code-only, no schema change, source layer stays SELECT-only, nothing touches v1. | 28 |
| - | ETL legacy author-column nulling: now that posts.created_by, posts.owner_user_id, briefs.created_by, post_versions.created_by, assets.uploaded_by are nullable, write NULL into these legacy author columns for migrated rows so ex-member provenance lives only in legacy_author_name (created_by/owner_user_id no longer pinned to operatorUserId). Code-only, no schema change, nothing touches v1. Depends on the nullability migration parity PR. Status: done. | 28 |
| - | ETL remaining author-column nulling: now that asset_versions.uploaded_by and asset_attachments.attached_by are nullable, write NULL into these columns for migrated rows so ex-member upload/attachment provenance is not pinned to the operator. Code-only, no schema change, nothing touches v1. Depends on the remaining-nullability migration parity PR. Status: queued. | 28 |
| - | ETL asset display_name backfill: populate assets.display_name during the asset migration with the attachment's parent-context label so the Assets card shows a meaningful name instead of the raw filename. source.ts adds the parent title to the three media batch SELECTs (posts.title, requests.title, and the joined post p.title AS post_title on comment attachments) and to the V1PostMedia/V1RequestMedia/V1CommentMedia row types (SELECT-only, nothing else changes). assets.ts adds a required MediaItem.contextLabel, an exported pure withKindSuffix(entityType, baseTitle) helper (brief -> "(brief)", comment -> "(comment)", post unchanged), threads display_name through pushAsset + appendEntityMedia, and sets the label per source in the now-exported collectMediaItems via the existing postTitle()/briefTitle() helpers so labels match the migrated titles exactly: post -> post title, brief -> brief title + " (brief)", comment -> the owning POST's title + " (comment)". display_name is added to ASSET_COLS so the set-based insert carries it; it is the ONLY new field written (byte fetch, dedup, R2, idempotency, author-NULL behaviour all unchanged). Tests cover the display_name plan output, withKindSuffix, and collectMediaItems label-per-source incl. the comment->post-title hop. Code-only, no schema change (assets.display_name already exists), source layer stays SELECT-only, nothing touches v1. | 28 |
| - | ETL R2 objectExists HEAD body fix: R2StorageClient.signedFetch always passed body to fetch, but objectExists issues a HEAD and Node's fetch rejects any GET/HEAD request that carries a body ("Request with GET/HEAD method cannot have body"), so the cutover byte-copy threw on the first image probe. signedFetch now drops the literal fetch body for GET/HEAD while the SigV4 payload hash (sha256 of the empty body) stays signed, so x-amz-content-sha256 is unchanged; PUT (putObject, ensureBucket) keeps its body. Regression test in tests/etl/r2-objectexists.test.ts injects a fake TracedFetch and asserts HEAD sends no body but still carries x-amz-content-sha256 (404 -> false, 200 -> true) while PUT still sends its bytes. Code-only, no schema change, nothing touches v1. | 28, 29 |
| - | ETL cutover validator: a read-only ETL command that proves the v2 load matches the v1 plan and that every migrated image object really exists in R2. New packages/etl/src/validate.ts exports validateCutover(deps) returning { ok, checks, digest } where deps inject everything (the five existing SourceDb counts, a read-only v2 countTargetRows, a readImageVersions over asset_versions joined to assets, objectExists, assetBucket) so it is fully unit-tested without a DB or R2. Checks: per-table counts plan (v1) vs target (v2) for posts/briefs/post_versions/comments (no deleted_at filter; equality required, reports plan/target/delta); asset shape (non-link rows carry r2_key+sha256+size_bytes+mime_type with size_bytes > 1 so the dev-seed stub of 1 fails, external_url null; link rows carry only external_url); bytes-present (objectExists for every non-link r2_key). digest is an informational sha256 over the sorted r2_key:size_bytes list, never a pass/fail. config.ts adds a --validate flag (Cli.validate, default false); validate is READ-ONLY so it does NOT require --confirm-cutover, while the same-database refusal and target-ref pinning stay in force. index.ts adds an async validate(config) runner + a main branch that builds SourceDb + a target Pool + R2StorageClient, resolves the workspace READ-ONLY by name (never ensureWorkspace), wires the deps (read-only COUNT(*) scoped to the workspace; presigned ranged-GET object probe), prints each check + the digest, sets process.exitCode = 1 on any mismatch, and closes source/pool in finally with no BEGIN/COMMIT and no writes. Tests under tests/etl cover all-match -> ok, a one-table count mismatch -> ok false naming the table + delta, an absent object -> ok false, a size_bytes=1 stub -> ok false, a well-formed link row passing, and the non-vacuous flips; the existing parseCli/assertSafe tests gain the new flag. No schema, no migration, no R2 write/delete, nothing touches v1. | 28, 29 |
| - | ETL legacy comment-author backfill (GBL): one-time, idempotent backfill that restores each migrated comment's original v1 author onto the v2 comments ONLY, into comments.legacy_author_name + legacy_author_email, without changing author_user_id (the FK stays the operator). At cutover loadComments attributed every comment to the operator, losing the real author; the v1 read-only DB still holds it. New packages/etl/src/backfill-comment-authors.ts adds a `backfill-comment-authors` subcommand (mode dry-run default / apply, uuid_v7 trace logged at entry), wired ONLY via a dispatch branch in index.ts. Reuses the v1/v2 connections + safety guards (sameDatabase, targetRefMatches) and the target workspace (by name) from config.ts; no hardcoded connection strings, project ids, or workspace uuid. Reads v1 once (paged 1000) joining post_comments -> posts -> profiles, derives via pure unit-tested helpers (legacyName: profile_name then btrim(author), truncate 120, empty -> null; legacyEmail: lower(btrim(author)) when a standard email else null, max 320; expectedBody via the existing commentBody scrub=false so it equals the stored v2 body), builds the active-member email set in ONE query and skips rows whose author is already a live member. Matches each v1 row to its v2 comment via a TEMP staging table + ONE set-based UPDATE on workspace + post + millisecond created_at + exact body + legacy_author_name IS NULL; counts 0/>1 matches (would-fill, skipped-member, no-match, ambiguous). dry-run reports counts and writes nothing; apply wraps staging + UPDATE in one transaction and rolls back + exits non-zero (ambiguous keys logged) on any ambiguous match. Separate read/write pools opened at entry and closed in finally. Posts and post_versions are intentionally out of scope (nothing recoverable in v1). New workflow_dispatch-only .github/workflows/backfill-comment-authors.yml (single mode input) reusing etl-run's secrets/env block + Node 22 + pnpm setup. Pure-function unit tests ride the existing etl CI. No schema change (legacy columns already exist on comments), source layer stays SELECT-only, nothing touches v1. | 28, 29 |

## 4. Phase 3: Frontend

Every frontend PR ships with an explicit light AND dark mode parity check. 44x44 minimum touch targets, no hover-only interactions.

| # | Title | Depends on |
| --- | --- | --- |
| 30 | App shell: BrowserRouter (Pipeline / Briefs / Chat / Activity / Assets / Settings / Recently deleted, "/" and unknown redirect to Pipeline), desktop Sidebar + mobile BottomTabs primary nav (Pipeline / Briefs / Chat / Activity), Topbar (workspace label, search trigger, Create menu, avatar menu), cmd/ctrl+K CommandPalette, AvatarMenu (switch workspace / Settings / Recently deleted / Profile / Appearance / Sign out via generic custom events), empty-first pages. Assets reached from Pipeline and Briefs heads, not primary nav. Built on the PR A UI primitives. | 2, 6 |
| 31 | Auth screens on the PR A primitives, rendered outside AppLayout on the radial-gradient background: /signin (email, password, Forgot link, Sign in via signInWithPassword, Create an account link) and /signup (name, email, password, workspace name, auto-detected editable timezone, Create account via signUp with display_name in options.data then workspaceCreate wrapper with p_trace_id). Session route guard: app routes redirect to /signin when signed out, /signin and /signup redirect to /pipeline when signed in, loading state while the session resolves. | 30, 8 |
| 32 | Discussion component shell: shared Composer, MessageRow, mention picker | 30 |
| 33 | Comments UI: Realtime thread, reactions, 5-min edit, soft-delete, Decision Records filterable view | 32, 13 |
| 34 | Create sheet: single mode, all fields visible, required title/caption/channel, optional asset gallery/format/target_date/owner/Origin (None or Brief), Save as Draft | 30, 11 |
| 35 | PCS: header, caption editor, asset gallery, metadata sidebar, three-tab comments, auto-version on edit, annotations (caption span + image pin, version-locked, prior-version greyed as copy changed), Approve button (PCS only, client must be logged in) | 34, 33, 11 |
| 36 | Pipeline: vertical kanban desktop, accordion mobile, owner + urgency colors, client visibility = all stages except Draft, filters, saved views, bulk actions (agency only, cap 10) | 35 |
| 37 | Briefs UI: client-only create, open/closed, comment + withdraw, read-only after creation, derived linked-post count, email-forward intake address | 32, 14 |
| 38 | Inbox UI: icon + badge, state chips (All / Unread / Snoozed), scope chips (Everything / Posts / Briefs / People / Groups / Clients), snooze (1h / 4h / tomorrow 9am / next week), inline reply, click navigates to PCS | 30, 16 |
| - | Chat UI foundation: frontend plumbing for Agora Chat with no chat screens. Add the agora-chat browser SDK, wire the optional VITE_CHAT_TOKEN_URL env (unset = chat unavailable, app unaffected) through env.ts + CI/deploy, an app-local src/lib/chat token fetch (POST { workspace_id } + Bearer session through fetchWithTrace, discriminated result, never throws), a useChatClient token+connection lifecycle hook (connecting/connected/unavailable, onTokenWillExpire renewal, signout + workspace-change teardown), and a ChatProvider context exposing { status, client } for later PRs. Availability gate: any failure resolves to unavailable and never breaks the app. Library code + tests only; not mounted (PR 39 mounts it). Restored after an earlier revert, now with the env fix: optional URL vars coerce an empty string (an unset GitHub Actions Variable) to undefined, so an unset VITE_CHAT_TOKEN_URL no longer fails url() validation and blanks the app. | 30, 17, 19 |
| - | Chat UI (shell): replaces the ChatPage stub with a master-detail channel list + message thread + plain-text composer, built on the merged Foundation context ({ status, client }). ChatPage mounts ChatProvider around the shell so the Agora connection lives only while /chat is open and tears down on navigate-away (App.tsx untouched). Status renders as connecting (loading) / unavailable (calm token-class panel, app unaffected, never throws) / connected (shell). The live thread reads from the Agora SDK only (getHistoryMessages + an onTextMessage handler under a separate 'chat-thread' event id, removed on channel change / unmount); the Postgres mirror is never on the read path. Channel list comes from the chat_channels registry via a new batched display-resolving read in src/lib/chat-reads.ts (one IN-clause read for group names, one for DM peer profiles, no N+1), ordered created_at desc; zero channels shows an actionless empty state. Sending uses the SDK send method (message.create injected via a small message-factory, keeping the SDK value import isolated). Agora username <-> Sorted id mapping is ported byte-for-byte into src/lib/chat/agora-identity.ts (round-trip parity test; dated TODO to extract a shared package). Responsive: list+thread side-by-side on md+, single-pane with a back affordance on small screens; token classes only, light/dark parity, 44px touch targets. Deferred: last-message preview, unread counts, "New chat" (PR4), attachments / post-share (PR5/PR6), mentions, voice notes, message context menu. | 32, 17, 19 |
| - | Chat UI (Groups/DM management): the channel-create and channel-management actions over the merged group/DM proc wrappers (groupCreate, groupRename, groupMemberAdd, groupMemberRemove, groupLeave, dmChannelEnsure), each minting a fresh trace at the action boundary and branching on the Result (inline error, never throws). A "New chat" entry point (channel-list header + a real empty-state action) opens a Sheet to either start a DM (pick one peer -> dmChannelEnsure -> open the channel) or create a group (name + multi-select members -> one groupCreate call, no per-member loop). A group info Sheet from the group header renames, adds members, removes a member (ConfirmDialog) and leaves the group (ConfirmDialog), showing current members with Avatar + name. Member pickers reuse the existing reads (workspace listMembers + batched readProfiles), excluding the current user from the DM peer list and existing members from the add-member list; a new batched listGroupMemberIds RLS select lands in src/lib/chat-reads.ts (chat_channels.entity_id now surfaced as ChannelSummary.groupId) for the member list with no N+1. Management is gated on the actor being a current group member (no finer capability key exists). Token classes only, light/dark parity, 44px touch targets, built on Sheet / Button / IconButton / Input / Field / Avatar / ConfirmDialog / EmptyState. Deferred: attachments / post-share (PR5/PR6), reactions / reply / forward / copy, search. | 39, 17, 19 |
| - | Chat UI (attachments): Photo + File sending in the message composer over the existing asset pipeline, reused unchanged: pick -> client size/MIME pre-check -> asset-upload worker, then the returned asset id(s) ride the Agora message as attachment_asset_ids in the SDK ext custom payload (set via the message-factory; the webhook mirror persists them, the live render path reads them off the SDK message). A config-driven paperclip menu (Photo / File, an append-only item list so PR6 adds Share-a-post without rewriting the menu) shows removable chips with per-file progress and inline upload failure (never throws); send carries attachment_asset_ids plus optional text, attachments-only is allowed, empty is blocked, and send is disabled while uploads are in flight. MessageThread dispatches per attachment by payload kind (image thumbnail vs file chip with open link, commented post-card extension point for PR6), presigning each id through the shared PresignCache via asset-presign (no N+1, no re-presign on re-render). No new route, no DB write or proc, no schema. Token classes only, light/dark parity, 44px touch targets, reusing Button / IconButton / Textarea / icons. Deferred: Share-a-post / post picker / post card (PR6), voice notes, reactions / reply / forward / copy, search. | 39, 17, 19, 15 |
| - | Chat UI (Share a post): a third composer action that shares one or more existing posts into a message as post cards, slotting into the PR5 extension points (the append-only attach menu and the explicit per-message render dispatch). The message carries only post id(s) in the SDK ext as shared_post_ids alongside attachment_asset_ids (no post content on the wire, no DB write/proc/schema; the webhook mirror persists the ext via raw_payload). A "Share a post" menu item opens a post-picker Sheet (search-all-posts title match + Review / Approved / All Posts chips defaulting to Review, all RLS-scoped via the existing @srtdio/posts read; new listPostsForPicker select) whose rows multi-select into removable shared-post chips in the composer; a send may carry text and/or attachments and/or shared posts, shared-posts-only is allowed, fully empty is blocked. Each viewer resolves a message's ids in ONE batched RLS IN read (new readPostsByIds), per viewer: an id RLS denies or that is deleted renders a "Post unavailable" card, no leak; a resolved card shows title + stage and taps through to /posts/:id via the existing router. No cover thumbnail (the existing posts read surfaces none and a new asset_attachments join is out of scope), so cards use a neutral placeholder. No new route, no schema. Token classes only, light/dark parity, 44px touch targets, reusing Sheet / Button / IconButton / Input / Field / Chip / EmptyState / icons. Deferred: cmd+K search (PR7), editing / approve / stage in chat, reactions / reply / forward / copy. | 39, 17, 19, 15 |
| 40 | Assets UI: fullscreen lightbox viewer (type-aware media for image / video / pdf / doc / xls / file, swipe + arrow + key navigation over the filtered list with links excluded), whole-card tap to open, long-press action sheet (Download / Copy link / Info), Info bottom sheet with delete, search + kind chips, toasts | 34, 15 |
| 41 | Trash UI: Recently deleted drawer entry, restore button + countdown | 40, 26 |
| 46 | Assets upload wired end to end: consolidated single-row toolbar (search + sort + add), add button 2-option menu (Upload files / Add link coming soon), AssetUploadSheet client pre-checks (100MB cap + shared MIME allowlist) before any bytes leave the device, sequential per-file upload to the asset-upload worker with Bearer JWT, per-file progress + retry, worker-error to plain-English toasts, reused=success, grid refresh on completion | 40, 15 |
| 42 | Search UI: cmd+K palette, scope chips (All / Posts / Briefs / Comments / Chat / Assets / People), Postgres FTS for entities + Agora SDK for chat, recency + relevance ranking | 38, 22, 39 |
| 43 | Workspace settings UI: name, timezone, default digest time, member roles, billing, 7-day soft-delete, ownership transfer (immediate, password confirm, owner cannot self-delete until transferred) | 30, 25 |
| 44 | Notifications (ephemeral): toast slide-in desktop + mobile, fires on Inbox triggers while user active, not stored | 38 |
| 45 | Cockpit UI at platform.srtd.io: passkey auth, dashboard, workspace management, actions (replay webhook, restart job, revoke sessions, billing override, extend trial, open ticket, global kill switch), impersonation banner + emergency-exit.html | 23, 30 |
| 47 | asset-upload worker CORS: handle OPTIONS preflight before auth (204 + reflected allowed-origin, methods POST/GET/OPTIONS, authorization + content-type + trace header, 24h max-age), attach CORS headers to every success and error response, mirroring asset-read exactly | 46, 15 |
| 48 | asset-upload worker link + rename routes: POST /links creates a link asset (kind=link, external_url set, byte columns null, version 1, current_version_id set) behind the same verifyCaller + isActiveMember gate, no R2; POST /rename updates assets.filename only behind a role gate (owner/admin/agency, client forbidden) reading the role in one membership query, leaving version rows and attachments untouched; both write audit rows (asset.create / asset.rename) with the verified actor and trace_id. Reuses the live auth, CORS, and error model | 47, 15 |
| 49 | Assets Add link + Rename wired in the frontend: enable the + Add menu "Add link" item opening a sheet (https:// link field + mandatory name, client-side validation, POST /links via the asset-upload client module, toast + grid refresh on success, worker-error to plain-English toasts), and a role-gated Edit affordance in the lightbox info sheet (owner/admin/agency only, role read once via workspace_members) turning the name into an inline input that POSTs /rename, updating the lightbox title and grid card without reload, 403 to the agency-only message | 46, 47 |
| 50 | Cleanup: record the live-applied service_role asset table grants (8 GRANTs for assets / asset_versions / workspaces / workspace_members / audit_log) as a documentation-only migration for fresh-database builds (already applied via MCP, GRANT idempotent, do NOT execute), and add the missing renameAssetInList unit tests flagged in the PR #105 audit | 48, 49 |
| 51 | Settings Profile panel wired into a working editor: prefill name/designation/photo from the loaded profile, reuse the onboarding AvatarCropper for an OPTIONAL photo change (re-pass the existing avatar_url when no new photo is picked), and save via user_profile_update preserving the existing email_opt_in (no toggle on this screen). Frontend only, no schema, no new fetch helper. deps: none | none |
| - | Shared section header (search, sort, create) extracted from Assets: one reusable SectionHeader chrome component (left title, controlled search, SortMenu, accent + action, slot for page-specific filter chips, all optional, command/query presentational) re-pointed from the Assets toolbar with zero visual or behavioural change. Pipeline/Briefs/Chat adopt it later. | 40, 46 |
| - | Pipeline, Briefs, Chat adopt shared search/sort/create header: drop PageHead/titles and render the shared SectionHeader (search + sort + accent "+") on all three, with each page's filter chips (Pipeline stage tabs, Briefs All/Open/Closed, none for Chat) in the slot. Pipeline/Briefs filter by title and sort Newest/Oldest/Target date over the in-memory list; "+" reuses the existing create events. Chat filters the loaded channel summaries by name client-side (no sort, no DB/Agora), keeps the "No conversations yet" zero-state and adds a distinct "No matches" state, "+" reuses onNewChat. Removes the dead Pipeline Sort button and decorative add-chips. Frontend only, no schema. | 36, 37, 39 |
| - | F1 shared PCS UI primitives plus semantic tokens: opt-in inline rendering mode on the shared icon wrapper (in-text icons sit beside text), an ActionRow bottom-sheet row, a dependency-free pointer useLongPress hook, and PCS annotation + stage semantic CSS vars (light/dark) mapped into Tailwind. Token-backed classes only, 44px touch targets, light/dark parity. Status: in-progress. | none |
| - | F2 PCS image gallery plus fullscreen viewer plus per-image download: PostDetailPage shows the post's images as an ordered thumbnail grid (new getPostGallery read over asset_attachments, one round trip with asset_versions + filename embeds, returns [] not null) above the caption; tapping a tile opens a view-only PostLightbox replicating the locked PCS prototype (dark backdrop, filename + counter top bar, 44x44 [pin][zoom][download][close] toolbar, inline image/video, arrows + swipe + ArrowLeft/Right wrap + Esc, bottom dot indicators). Per-image download mints an attachment-disposition presigned URL (optional disposition param added to requestPresignedUrl, inline default byte-for-byte unchanged). Inert F5 seams (pinOverlay + onRequestPin) wired to safe defaults. No add/reorder/remove (F7), no annotations (F5). Neutral overlay tokens added (light/dark parity). Token-backed classes only, 44px touch targets. Status: in-progress. | 15, 40 |
| - | F3 brief reference-image gallery on BriefDetailPage reusing the merged F2 viewer: BriefDetailPage shows the brief's uploaded reference images as an ordered, view-only gallery (new getBriefGallery read over asset_attachments where entity_type='brief', one round trip with asset_versions + filename embeds, returns [] not null) above the brief body, with the caption line "Reference images from the client. Tap for full screen or download. Briefs are read-only after creation."; tapping a tile opens the reused F2 PostLightbox (view + download only, pins inert for briefs). The shared PostGallery gains backward-compatible columns / aspect / showIndex props (defaults leave the post gallery byte-unchanged); briefs render a 2-column 3/2 grid with no per-tile index badge. When a brief has no images the gallery section renders nothing. No schema, no migration, no RPC. Token-backed classes only, light/dark parity, 44px touch targets. Status: in-progress. | 37, 15, 40 |
| - | F4 caption-highlight annotations on PostDetailPage: selecting caption text opens a bottom-sheet composer that posts a comment anchored to the span (comment-first then annotationCreate on one shared trace; an annotation failure leaves the comment as a plain comment with an error notice, no retry). Existing caption_span annotations render as amber highlights (F1 annotation token) with a superscript number; clicking a highlight scrolls to its linked comment, and the comment shows a chip back to the caption. Offsets are computed by walking the caption's text nodes (never indexOf), excluding the unselectable superscript marker, and every highlight is bounds-guarded against the live caption. Annotations made on an older post version render greyed as "copy changed · v{n}" with their original snapshot quote, never as live highlights. Comments gains optional, backward-compatible annotation chip + refresh-signal props (briefs render no chips, unchanged). No schema, no migration, no RPC, no package edits. Token-backed classes only, light/dark parity, 44px touch targets. Status: in-progress. | 15, 40 |
| - | F5 image-pin annotations in the PCS lightbox: the [pin] button arms placement and a tap on the image anchors a comment to a normalised (x,y) point on the current slide (comment-first then annotationCreate image_pin on one shared trace; an annotation failure leaves a plain comment with an error notice, no retry). Current-version pins render as numbered dots over the right slide (matched by asset_attachment_id, coords clamped to 0..1, out-of-range skipped); tapping a dot scrolls to its linked comment, and the comment shows a chip back to the pin. Pin display numbers continue F4's caption sequence (captions keep their numbers, pins take the next). Placement is disabled while zoomed and coordinates come off the rendered image rect. Pins made on an older post version render greyed "copy changed", never as live dots. PostGallery gains optional, backward-compatible pinOverlay / onRequestPin / onPlacePin pass-through props (briefs unaffected). No schema, no migration, no RPC, no package edits. Token-backed classes only (F1 annotation amber), light/dark parity, 44px touch targets. Status: in-progress. | 15, 40 |
| - | F6 PCS comments overhaul: bring post comments to the locked spec while keeping brief comments byte-unchanged. One-level threads (roots vs replies grouped in memory from the flat list, a collapsed "N replies" expander, a reply composer posting createComment with parent_comment_id, no reply-to-reply affordance); a muted "Comment deleted" tombstone for a soft-deleted parent that still has a live reply (a deleted leaf is omitted); author-only edit / delete on the caller's own live comment (editComment / deleteComment with a POSITIONAL traceId, an "edited" marker on edited_at, the current user id read from the session not a prop); comment attachments sent through the reused chat upload path (asset VERSION ids passed as attachment_asset_ids) and rendered through the shared MessageAttachments + PresignCache (image thumbnail vs file chip, mime from a batched asset_versions read, no N+1); author names and @[uuid] mentions resolved to display names in one batched users read (a new commentProfiles helper), unresolved to "(ex-member)". The composer is extracted to CommentComposer and reused by the root and reply composers. The read path relaxes the post/brief listing filter so soft-deleted rows return for tombstoning (listComments only; searchComments unchanged). All six F4/F5 seams preserved (commentDomId, annotationChip, annotationsByCommentId, onAnnotationChipClick, refreshSignal, CommentAnnotation). No schema, no migration, no new proc, no RPC change. Token-backed classes only, light/dark parity, 44px touch targets. Status: in-progress. | 13, 35 |
| - | F7 PCS metadata editing plus gallery slide-actions plus role-gated stage rail: PostDetailPage gains agency-side inline title edit (postUpdate, no new version), a tappable owner/date/origin sub-header opening a Post details bottom sheet (Bucket picker over a new listBuckets read of non-archived workspace_buckets, Owner picker over active members, native target-date overlay, read-only Origin; client read-only with a hint, owner always resolved to a display name or "(ex-member)", never a raw uuid), and an agency-side caption pencil (post_caption_update, validate 1..5000, auto-versions). Gallery slide-actions (agency only) open via long-press, right-click, and a lightbox kebab over pure, unit-tested transforms (moveLeft/moveRight/append/insertAfter/removeAt; removeAt never yields []) committed through gallerySet (auto-versions + re-stales annotations server-side); add reuses AssetUploadSheet with the upload versionId surfaced on UploadOutcome; a dashed Add tile appends; removing the last image is blocked with a toast; per-thumbnail pin-count badge. The stage rail is driven by a pure, unit-tested visibleStageActions(stage, role) matching the server exactly (client: Approve / Request changes; agency: Park / Send-or-move to review), falling back to a read-only status line. No schema, no migration, no RPC or package edits; the existing wrappers and stageTransition are unchanged. Token-backed classes only, light/dark parity, 44px touch targets. Status: in-progress. | 35, 13 |
| - | F7.5 PCS version-history viewer (read-only; zero schema, zero new writes, zero new RPC): a vN pill on the PostDetailPage header (current version_number, hidden when a post has no versions, 44x44) opens a Version history bottom sheet listing every immutable version newest-first (v{n}, author display name resolved via the same active-members read PCS already uses with an (ex-member) fallback for a null/non-member author, a localized timestamp via the app's existing toLocaleString convention, a marker on the current version); tapping an older row enters a PCS-wide READ-ONLY MODE that suppresses every F7 edit affordance (inline title, Details sheet, caption pencil, gallery add/reorder/remove, stage rail) and renders the version's caption read-only plus a "{count} image(s)" gallery summary (no existing read resolves bare asset_version_ids to thumbnails, so a summary line is used, not a new proc/read) under a literal "Versions are immutable" banner with a "Back to v{current}" control; the current row just closes the sheet. New pure helper src/lib/post-versions.ts (parseSnapshot defensive over the canonical v2 {caption,gallery} snapshot and legacy/malformed ETL snapshots, never throwing; currentVersionNumber; sortVersionsDesc; toVersionViews) with colocated tests. getPost (packages/posts/src/reads.ts) keeps its existing embedded versions field untouched and only gains a deterministic version_number-ascending order on the post_versions embed (additive, same single query, no N+1, no new field). Read-only mode is the single write gate: no postUpdate/postCaptionUpdate/gallerySet/stageTransition is reachable while active. Token-backed classes only, light/dark parity, 44px touch targets. Status: in-progress. | 35, 13 |
| - | F8 PCS post action sheet (frontend only; zero schema, zero Postgres write, zero new RPC, zero new dependency): a "more" trigger (44x44) on the live PostDetailPage header opens a bottom sheet with three actions, mounted in live PCS only (never the read-only old-version view). Send to chat opens a conversation picker populated by the existing listChannelSummaries (the Chat page's read) and sends the current post into the tapped conversation via the existing Agora sendText with sharedPostIds=[postId] (not a Sorted RPC, so no Postgres write and no new trace), with success/failure toasts and a "No conversations yet" empty state; the Agora connection is scoped by a ChatProvider mounted around the picker, mirroring the Chat page. Copy link writes an in-app authenticated deep link (window.location.origin + the existing postRoute, no domain hardcoded, no share token: there is no public-share concept in the MVP) to navigator.clipboard. Download all images loops the same attachment-disposition presigned URL the PCS lightbox already uses per image (requestPresignedUrl over the existing asset-read path, resolved up-front then triggered sequentially), hidden when the gallery holds no image. New pure helper src/lib/post-share.ts (buildPostLink tolerant of trailing/leading slashes; canDownloadAll; toDownloadDescriptors ordered + URL-failure-dropping) with colocated tests. listChannelSummaries, sendText, and the asset read path are imported and used as-is, never modified. Token-backed classes only, light/dark parity, 44px touch targets. Status: in-progress. | 35, 13 |
| - | Pipeline card thumbnails (data only): listPosts (packages/posts/src/reads.ts) now returns PipelinePost = Post & { thumbnailAssetVersionId: string \| null }, each row carrying the asset_version_id of its first image attachment so the Pipeline card can presign a thumbnail. Resolved in ONE additional batched query (firstImageByPost) over asset_attachments joined to asset_versions for every returned post id (entity_type='post', deleted_at IS NULL, inner join + mime LIKE 'image/%', ordered entity_id/position/attached_at ascending, reduced to a Map<postId, assetVersionId>); never one read per post (no N+1). "First image" = live image-mime attachment with the lowest position, tie-broken by earliest attached_at; posts with only video/link/file or no attachment carry null. Empty board returns [] with no attachments round trip. Existing limit/keyset/order untouched; no thumbnail column invented. No schema, no migration, no generated-types or RPC change; card/presign/worker wiring is out of scope. Tests in tests/posts assert the single batched query for N posts, the lowest-position pick, and the video/none/soft-deleted null cases. | 36, 15 |
| - | Briefs read returns thumbnail data: listBriefs (packages/briefs/src/read.ts) now returns BriefWithThumbnail = Brief & { thumbnailAssetVersionId: string \| null }, each row carrying the asset_version_id of its first reference image so the Briefs card can presign a thumbnail. Mirrors the posts read's private firstImageByPost (which is NOT exported, so it is re-implemented here, never imported from packages/posts): ONE additional batched query (firstImageByBrief) over asset_attachments joined to asset_versions for every returned brief id (entity_type='brief', entity_id IN the result ids as plain TEXT with NO uuid cast since brief.id is a JS string, deleted_at IS NULL, inner join + mime LIKE 'image/%', ordered entity_id/position/attached_at ascending, reduced to a Map<briefId, assetVersionId>); never one read per brief (no N+1). "First image" = live image-mime attachment with the lowest position, tie-broken by earliest attached_at; briefs that are link-only or have no image attachment carry null (the documented "no image" signal, not an error). Empty result returns [] (never null) with no attachments round trip. Existing filters/order/pagination untouched; the briefs Row is unchanged (thumbnail is DERIVED, not a column), so no generated-types regen. No schema, no migration, no RPC change; card/presign/worker wiring is out of scope. BriefWithThumbnail is exported via packages/briefs/src/index.ts. Tests in tests/briefs assert the single batched query for N briefs, the lowest-position pick, and the link-only/none/soft-deleted null cases. | 14, 15 |
| - | Brief create attachments (backend): brief_create now accepts an optional payload field attachment_asset_version_ids, an ordered array of asset_version ids of any kind (image / video / pdf / Office-doc / link). Briefs are read-only after creation, so creation is the only attach point: each id is bound to the new brief as an asset_attachments row with entity_type='brief', entity_id = the brief id, position = array order. The proc signature is UNCHANGED (p_workspace_id, p_payload, p_trace_id) so generated types do not regen; the field rides inside p_payload. createBrief (packages/briefs/src/create.ts) gains the optional attachmentAssetVersionIds?: string[] mirrored into the payload, and the brief-create edge function validates it is an array of non-empty strings before threading it through. The proc re-validates: a non-array, a non-uuid element, or a cross-tenant / missing asset_version raises invalid_payload and writes no brief and no attachments. Migration recorded as already-applied-to-live (record only, do NOT execute). Tests in tests/briefs cover all three kinds attached in order, the empty/absent no-attachment case, cross-tenant rejection, and both malformed shapes. Status: done. | 14, 15 |
| - | Pipeline card thumbnails (card UI): PostCard (src/components/pages/PostCard.tsx) becomes thumbnail-forward over the data PR's PipelinePost (Post & { thumbnailAssetVersionId: string \| null }), reusing the assets-grid presign recipe unchanged (useInView + the shared PresignCache peek/resolve + imageTileState) with no new helper, no second presign call site, and no forked thumbnail logic. New PostCardProps { post: PipelinePost; cache: PresignCache; presignEnabled: boolean }: the card receives the board's one shared cache and never instantiates its own, never calls requestPresignedUrl or the worker directly. A square frame (aspect-square, object-cover, rounded 10) renders, when thumbnailAssetVersionId is non-null AND presignEnabled, a lazily-presigned image gated by useInView (shimmer / image / fallback-glyph via imageTileState, <img onError> falls to the glyph); a null-thumbnail text post shows an italic ~4-line caption snippet (empty caption falls to the glyph); any other null-thumbnail post shows the glyph; a carousel post adds a small stacked-squares badge top-right (no count query, only fields PipelinePost already carries). Below the frame: title (1-line ellipsis) then platform Chip + format Chip + right-aligned target_date via the existing formatTargetDate. Whole-card overlay nav to /posts/:id unchanged; token classes only, light/dark parity, 44px tap target. Colocated SSR tests cover the image, caption/empty-caption, non-text glyph, and presign-disabled (cache untouched) paths; auto-run by the existing CI test job (pnpm test globs *.test.tsx). Making cache + presignEnabled required intentionally leaves PipelinePage a type error wired by the board PR (own a PresignCache, pass cache + presignEnabled, widen state to PipelinePost[]); PipelinePage, packages/posts, asset-presign, AssetCard, the worker, migrations and generated types untouched. | 15, 36 |
| - | Pipeline card thumbnails (board wiring): minimum wiring so the card-UI PR's required PostCard props type-check and thumbnails go live on the existing board, with NO board rebuild (tabs/feed/kanban/move-sheet stay a later PR). PipelinePage (src/components/pages/PipelinePage.tsx) widens its posts state from Post[] to PipelinePost[] (listPosts already returns PipelinePost[], so setPosts is unchanged), owns ONE shared PresignCache via useMemo with empty deps constructed exactly like AssetsPage (endpoint = env.VITE_ASSET_READ_URL ?? null, getAccessToken from the supabase session, fetcher = fetchWithTrace) plus presignEnabled = env.VITE_ASSET_READ_URL !== undefined mirroring AssetsPage, and passes cache + presignEnabled to every PostCard so all cards share one cache and its concurrency cap (no cache per card/render, no page-level network call; cards still lazy-resolve via useInView). groupByStage is typed over the base Post and widens the element type back, dropping thumbnailAssetVersionId, so the grouped result is asserted back to Record<Stage, PipelinePost[]> (the same listPosts objects; no grouping/columns/tabs/sort/search/count/nav change). PostCard, packages/posts, asset-presign, AssetCard, use-in-view, assets, the worker, migrations and generated types untouched; no schema, no MCP. Existing pipelineHeader test unaffected (does not touch the card); typecheck + build + tests green. | 15, 36 |
| - | PCS cosmetic restyle of PostDetailPage and PostGallery to the locked v6 prototype layout (structure, order, chips, cards, gallery, helper text), using the app's current fonts and design tokens only (no new fonts, no accent change; font face and exact hues are a separate PR): the app bar collapses to three slots, back / centre-truncated display title (read-only here) / kebab, with a 44x44 spacer in read-only so the title stays centred; an always-present header block carries a chips row (a data-driven bucket chip with an inline colour dot, agency-clickable to the Details sheet and the one allowed inline style, rendered only once the bucket resolves; a soft-filled stage pill with In review / Approved / etc. labels; plain platform dot format), the large body title (agency inline-edit with a trailing pencil glued to the last word, else a plain h1), and the unchanged owner / date / origin Details button; the gallery section drops its label and its in-grid dashed Add tile (first-image Add now lives in the empty state, later adds via the lightbox kebab) and gains a role-specific helper line under the grid; the caption, action, and comments sections each become a rounded border card; the caption card header carries the moved version pill, a both-roles Copy button, and the agency pencil, with a select-to-comment hint under the body; the action card drops its label and keeps the pure role-gated stage rail with the review helper copy, tinting the Approve target green from the good token; the comments card gains a long-press hint over the unchanged Comments; the gallery slide counter moves from top-left to bottom-left. Frontend only, no schema, no migration, no RPC, no package edits, no token or font changes; does not read stage_entered_at. The good/warn/bad stage pills fall back to a coloured border (their tokens are bare hex CSS vars, so Tailwind's /opacity modifier cannot mint a soft tint and there is no good/warn/bad-soft token). Token-backed classes only, light/dark parity, 44px touch targets. Status: in-progress. | 51 |
| - | Pipeline board LAYOUT rebuild (structure only; move/drag LOGIC and the move sheet stay PR4): PipelinePage swaps its plain columns for two responsive surfaces under new src/components/pages/pipeline/, picking between them with the app's existing useMediaQuery at the md breakpoint (>=768px). Desktop PipelineBoard is a horizontal kanban, one fixed-width (260px) column per stage in the locked STAGE_TRANSITIONS order (draft, review, approved, parked, rejected), each header a stage dot + label + count, capped at 10 with a foot "View all N" control and an "Empty" placeholder; columns are drag-READY containers and cards drag-READY items (data-drag-container / data-drag-item / data-post-id) but NO drag handler, onDrop, or stageTransition is wired. Mobile PipelineFeed is tab-driven: the shared SectionHeader stage tabs (All + each stage, count badge in the label since the Tabs primitive renders a label, reused unchanged) drive an All view of grouped sections (each capped at 10 with a View all that switches the active tab) or a single-stage view (uncapped 2-col grid, EmptyState "Nothing in {Stage}. Posts land here when moved." when empty); cards reserve an inert long-press gesture target (// TODO(PR4)). PostCard is reused unchanged and the page keeps its single shared PresignCache + presignEnabled, search (filterByTitle), and sort (useSort/sortByDate) passed down untouched (no new fetch, no N+1, cards still lazy-resolve via useInView). Stage dot colours come from the existing stage-* tokens (tailwind.config.ts + index.css), draft using the neutral fg-3 token (no new hex, no token added). groupByStage (src/lib/post-board.ts) is made generic over the post shape so the grouped result keeps PipelinePost (thumbnailAssetVersionId) with no `as` assertion. Colocated SSR tests walk the board/feed trees (All cap at 10, View all affordance + wiring, single-stage uncapped, empty EmptyState message, kanban column order), auto-run by the existing vitest *.test.tsx glob. Token-backed classes only, light/dark parity, 44px touch targets, no hover-only interactions. No schema, no migration, no RPC or package edits; PostCard, packages/posts, asset-presign, AssetCard, the worker, migrations and generated types untouched. Status: in-progress. | 36 |
| - | Pipeline stage-move flow (final pipeline PR; move/drag LOGIC onto the existing board structure): wires stage moves onto the layout PR's drag-ready surfaces with NO new dependency (native HTML5 DnD only). Desktop PipelineBoard cards become draggable (dataTransfer carries "fromStage:postId") and columns become drop targets; a drop onto a target legal per canTransition calls up to the page move handler, an illegal drop bounces (no state change, no error toast) and the dragover affordance shows on legal targets only (a module-scoped source-stage drives the cue since dataTransfer is unreadable during dragover; the payload is re-read + re-checked on drop). Mobile PipelineFeed cards get a press-and-hold long-press (reused src/components/ui/useLongPress) opening a new MoveSheet bottom sheet (reused Sheet primitive) listing every OTHER stage, legal targets enabled and the rest disabled with a Blocked hint; a short tap still navigates to PCS (the trailing click is swallowed in the capture phase). PipelinePage owns the single move handler runMovePost: it AWAITs stageTransition (no optimistic flip), on success flips the post's local stage so the existing useMemo re-groups it + closes the sheet + fires a success toast '"{title}" moved to {Stage}', on failure keeps the post put + toasts a friendly message mapped from invalid_stage_transition / forbidden_role / workspace_member_only (raw codes never leak); a per-post in-flight Set guards double-fire so a slow network can't double-submit. Toasts reuse the existing Assets useToasts + Toasts surface (no new toast system). StageDot is de-duped into stage-meta (renamed .ts -> .tsx, exported, imported by both board surfaces + the sheet); the All-view per-section empty placeholder is left as-is. Board/Feed only detect the gesture/drop and call up via props; no stageTransition call lives in them. Colocated tests cover the move success/failure/re-group/toast paths, the double-fire guard (one proc call), MoveSheet enabled/disabled rows per the transition map (two source stages), and a desktop invalid drop not calling the handler; auto-run by the existing vitest *.test.tsx glob. Native events only (no DnD library), token-backed classes only, light/dark parity, 44px touch targets. No schema, no migration, no RPC or package edits; PostCard, packages/posts, asset-presign, AssetCard, the worker, migrations and generated types untouched. | 36 |
| - | Pipeline cards + stage switcher redesign (frontend only; no schema, no DB, no type regen): the PostCard meta collapses its two stacked platform/format pills into one quiet line (a new PlatformMark lowercase letter badge + a format glyph + the right-aligned target date), and the imageless tile becomes a format-aware premium fallback (new ThumbnailFallback kind 'post' on the shared Thumbnail: a from-panel-2/to-panel-3 gradient carrying a format eyebrow + a clamped body that is the caption else the title; text-forward layout for text/image/carousel, centred 'media' layout with a large glyph for video/link) so a no-creative post never shows the broken-looking file icon; video posts WITH a poster get a centred play badge (gated on thumbnailAssetVersionId so it never doubles with the media-fallback glyph). New format-icon helper (FormatGlyphToken + FORMAT_GLYPH_LABEL + formatIcon) over new icons (IconText / IconImage / IconCarousel / IconVideo / IconPlay, strokeWidth 1.7); single_image folds to the 'image' token. The stage switcher swaps the underline Tabs strip for a new StageChips horizontal chip bar mirroring ActivityFilterBar (flex-nowrap overflow-x-auto, the same alpha edge-cut fade mask + ResizeObserver, replicated not imported), each chip a 44px pill with the stage dot + label + discrete count (accent-soft/text-accent/border-accent-line when active, aria-current); PipelinePage builds {key,label,count} items and stops importing Tabs (Tabs.tsx kept for other surfaces). The mobile feed becomes one flat capped grid: BOARD_CAP drops 10 -> 6 and PipelineFeed renders the active stage's first page with an in-place "Show N more" Button (>=44px) instead of grouped sections + "View all", the shown count resetting on stage change; PipelinePage splits its memo (sorted -> grouped -> counts) and feeds the flat sorted list, the desktop PipelineBoard call unchanged. Token classes only (no hex, no dark: literals; light/dark parity automatic), 44px touch targets on every chip + Show more. Thumbnail's existing file/link/glyph/text branches are untouched (Briefs still uses 'text'); Assets and Briefs files untouched. Colocated tests updated to the new meta line + 'post' tile, the StageChips items, and the flat feed + Show more; auto-run by the existing vitest *.test.tsx glob. No schema, no migration, no RPC or package edits; supabase.generated.ts untouched. | 36 |
| - | Shared thumbnail module (DRY the tile AssetCard and PostCard each forked): new src/components/media/ holds ONE authoritative tile. use-thumbnail.ts generalizes the two private hooks (AssetCard's useThumbnail, PostCard's usePostThumbnail) into useThumbnail({ assetVersionId: string \| null, cache: PresignCache, enabled }) returning { ref, url, failed, onError }: lazy via the imported useInView, warm-served from the imported PresignCache.peek, refreshed before expiry, and inert (no peek/resolve) when disabled or the id is null. Thumbnail.tsx is the tile: props { assetVersionId, cache, presignEnabled, aspect?: 'square'\|'4/3' (default square), fallback, alt, children } where fallback is a discriminated ThumbnailFallback (link+label / file+extension / text+caption / glyph) that unifies AssetCard's Fallback with PostCard's GlyphTile+CaptionTile into one renderer; it shows a shimmer / a full-bleed <img object-cover> / the fallback, with the tile background switching by state only (image=bg-panel-2, fallback=bg-panel-3) on a SINGLE tile and no inner rounding/padding so the caller's overflow-hidden+radius keeps it edge-to-edge. index.ts barrels Thumbnail + useThumbnail + the type. AssetCard consumes it at aspect='4/3' (image asset -> assetVersionId; link -> fallback link+linkDomain; doc -> fallback file+fileExtension): Assets is visually UNCHANGED. PostCard consumes it at aspect='square' and goes FULL-BLEED (card gains overflow-hidden, the p-3 moves onto the title/meta block) so the image is edge-to-edge like AssetCard; thumbnailAssetVersionId -> image, null+text+caption -> text fallback, null+link -> link glyph, else glyph; carousel badge passed as children, nav to /posts/:id and props unchanged so the board wiring still type-checks. After this, single_image, text and glyph posts share ONE tile background by state (the old 3-background inconsistency is gone). imageTileState, PresignCache, useInView imported as-is; no second copy, no new dependency, no cache created in the module. Colocated Thumbnail.test.tsx (img/object-cover, link/file/text/glyph fallbacks, disabled+null-id never peek/resolve, aspect default+4/3) plus new AssetCard.test.tsx and the existing PostCard.test.tsx assert the same observable output, auto-run by the existing vitest *.test.tsx glob. Token-backed classes only, light/dark parity. No schema, no migration, no RPC; packages/posts, packages/briefs, asset-presign, use-in-view, src/lib/assets, the worker, migrations, generated types and all root files untouched. | 15, 36 |
| - | Pipeline polish (layout + honest counter; frontend only, no schema, no new deps): three layout/UX fixes on the rebuilt board. (1) Desktop two-axis scroll: the PipelineBoard horizontal scroll container (overflow-x-auto) gains overflow-y-hidden + min-h-0 so only the horizontal axis scrolls and vertical scrolling stays owned by the app-shell main, instead of a nested both-axis scroll fighting the page. (2) Mobile horizontal leak: on ~160px 2-col PostCards the platform/format/date chip row pushed past 100vw; the chip row gets flex-wrap + min-w-0, the card root gets min-w-0 (plus its existing overflow clip), and the platform + format chips carry min-w-0 + shrink + truncate so they wrap/clip rather than widening the grid track. Chip.tsx gains an optional, additive className passthrough (appended last via cn, default undefined so every other consumer renders identically) since a chip-level shrink/truncate cannot be applied from PostCard alone. (3) Honest header counter: the "N posts" line read the unfiltered posts.length and never moved while searching; it now reads the FILTERED total (counts.all, summed from the same grouped list the board/feed derive) via extracted pure helpers stageCounts + postCountLabel, reusing the existing filter and never the per-stage 10-item display cap. Behavioral test asserts the counter reflects the filtered total under an active search and the full total when empty (fails if it reverts to the unfiltered length); structural tests assert the board scroll container carries the vertical-lock class and the PostCard chip row carries wrap + chips carry shrink/truncate; auto-run by the existing vitest *.test.tsx glob. Layout-only: no color tokens changed, no new colors or hardcoded hex, light/dark parity unaffected, 44px touch targets unaffected. No schema, no migration, no RPC or package edits; packages/*, media/*, AssetCard, PipelineFeed, MoveSheet, workers, generated types and all root files untouched. | 36 |
| - | Pipeline polish: widen search scope + complete the sort set (frontend only, no schema, no new deps, no read/server change). (1) Search scope: the Pipeline post filter matched title only; it now matches title + caption + platform (case-insensitive, trimmed) via a new generic src/lib/list-sort.ts helper filterByFields(items, query, fieldAccessor) that filterByTitle now delegates to. Briefs keeps its own title-only filterByTitle path, unchanged. (2) Full sort set: Pipeline offered only Newest / Oldest / Target date; the locked set is Recently updated (updated_at desc, NEW), Newest (created_at desc), Oldest (created_at asc), Target date (target_date asc, nulls last), Title A-Z (title asc, case-insensitive locale-aware, NEW), via a new PostSort type + POST_SORT_OPTIONS + sortPosts comparators with a stable id tiebreaker so equal keys are deterministic; target_date nulls-last preserved. Pipeline re-sorts the already-loaded in-memory list (all fields already on the Post row), so NO listPosts / server / schema / RPC change. Briefs keeps the date-only DateSort / sortByDate / DATE_SORT_OPTIONS path untouched. Tests in list-sort.test.ts cover title/caption/platform/no-match/case-insensitive search (fails if scope narrows back to title-only), each of the 5 comparators incl. Recently-updated desc, Title A-Z case-insensitivity, Target-date nulls-last, and the id tiebreaker, plus a guard that the Briefs title-only path is unchanged; auto-run by the existing vitest glob. No color/layout change, light/dark parity and 44px touch targets unaffected; packages/*, media/*, AssetCard, PostCard, PipelineBoard, workers, generated types and all root files untouched. | 36 |
| - | Briefs card thumbnails (card UI): BriefCard (src/components/pages/BriefCard.tsx) prepends the shared square Thumbnail (@/components/media, aspect="square") as a leading 64px tile, mirroring how PostCard consumes it, additive over the merged "Briefs read returns thumbnail data" BriefWithThumbnail (Brief & { thumbnailAssetVersionId: string \| null }); the existing fields (title, objective snippet, status dot, target date, linked-posts count, inline Close action) are unchanged, now sitting in a min-w-0 flex-1 column beside the tile inside a rounded + overflow-hidden wrapper that clips the edge-to-edge tile. Selection order per brief: thumbnailAssetVersionId present -> presigned image (assetVersionId + the shared cache + presignEnabled into Thumbnail, same as PostCard); else the FIRST usable reference link -> { kind: 'link', label: <domain> }; else objective/title text -> { kind: 'text', caption }; else { kind: 'glyph' }. reference_links is INCONSISTENT jsonb (Json | null) so a local defensive parser (no any, narrowed from unknown; NOT BriefDetailPage.toLinks, not imported) handles all three known shapes: string[], array of { url }, and the ETL object { drive_link, images }, returning the first string that parses as an absolute URL and deriving its www-stripped hostname; nothing parseable falls through to text/glyph. Wiring fix: BriefsPage (src/components/pages/BriefsPage.tsx) previously built NO PresignCache so image thumbnails could never resolve; it now owns exactly ONE PresignCache via useMemo (endpoint = env.VITE_ASSET_READ_URL ?? null, getAccessToken from the supabase session, fetcher = fetchWithTrace) plus presignEnabled = env.VITE_ASSET_READ_URL !== undefined, copied from BriefDetailPage WITHOUT editing it, and threads that single cache + presignEnabled into every BriefCard via an extracted pure briefCardList helper (one cache for the whole list, never per-card). The state widens Brief[] -> BriefWithThumbnail[] (listBriefs already returns it) and the status filter result is re-narrowed back to BriefWithThumbnail via an id map (filterBriefsByStatus is typed over bare Brief), no cast. Colocated SSR tests: image path consumes the assetVersionId; one link-fallback test per reference_links shape (string[], [{url}], ETL {drive_link}) asserts the right domain; objective text fallback; glyph fallback; existing fields survive; and a structural briefCardList test asserts one card per brief all sharing the identical cache instance with presignEnabled + per-brief closing/error/open wiring; auto-run by the existing vitest *.test.tsx glob. Token-backed classes only (bg-panel/-2/-3, border, text-fg-2/fg-3, rounded-*, ring-accent), no hardcoded hex, light/dark parity, 44px touch targets unaffected, verified on a ~360px viewport. No schema, no migration, no RPC, no new dep; packages/*, BriefDetailPage, media/*, PostCard, AssetCard, workers, generated types and all root files untouched. | 14, 15 |
| - | Pipeline stage-move hardening (frontend only, no schema, no new deps; two tightly-related fixes carried over from the stage-move flow PR). (1) Error toast on transport failure: runMovePost (src/components/pages/PipelinePage.tsx) had a try/finally with NO catch, so a transport-level throw from stageTransition (network drop, RPC failure that rejects rather than returning a Result) surfaced nothing while the domain-error path already toasted. A catch now fires the existing Assets useToasts toast (same push the success and domain-error paths use, no new toast system) with a shared MOVE_FALLBACK_MESSAGE constant (also the moveErrorMessage default). No optimistic stage flip exists (the local stage update runs only AFTER a successful proc) so the failure path leaves the board consistent with nothing to revert; the finally still clears the per-post in-flight Set guard exactly as on success. (2) Wire MoveSheet busy: MoveSheet already CONSUMES busy (disabled={!allowed || busy}) but PipelinePage never passed it. The page adds a reactive movingId state (the in-flight guard is a ref, so it cannot drive a re-render on its own), set when movePost actually starts a move and cleared in the returned promise's finally, mirroring runMovePost's guard so a double tap is dropped without disturbing busy; MoveSheet receives busy={movePostTarget !== null && movingId === movePostTarget.id}, so the sheet's move actions disable during an in-flight move. MoveSheet's only consumer is PipelinePage. Colocated SSR tests: runMovePost with stageTransition rejecting at transport level toasts MOVE_FALLBACK_MESSAGE, makes no stage change, does not close, and clears the in-flight guard (fails if the catch is removed); the existing success-toast and double-fire-guard tests are unchanged (no regression); MoveSheet disables every target when busy and keeps legal targets enabled when not (fails if busy is ignored). Auto-run by the existing vitest *.test.tsx glob. No color/layout redesign, token-backed classes only (no hardcoded hex), light/dark parity and 44px touch targets unaffected. No schema, no migration, no RPC or package edits; packages/*, media/*, AssetCard, PostCard, BriefCard, BriefsPage, PipelineBoard layout, workers, generated types and all root files untouched. | 36 |
| - | Inbox read-state procs (schema foundation for the Activity write path): record the live-applied per-user read-state + snooze SECURITY DEFINER procs as a retroactive migration and regenerate supabase.generated.ts from the v2 project. Three user-callable procs, all gated on is_active_workspace_member, search_path '' hardened, REVOKE FROM PUBLIC + GRANT EXECUTE TO authenticated: inbox_mark_read(p_entry_id, p_workspace_id, p_created_at, p_trace_id) clears read_at on the caller's own unread, non-deleted entry within the p_created_at month partition; inbox_mark_all_read(p_workspace_id, p_trace_id) clears read_at on all the caller's unread, non-snoozed, non-deleted entries; inbox_snooze(p_entry_id, p_workspace_id, p_created_at, p_kind, p_trace_id) validates p_kind in (1h, 4h, tomorrow_9, next_week, clear) and sets snoozed_until in the workspace timezone (UTC fallback). Each emits audit_log_write only when a row changed. trace_id is an explicit param throughout. Schema already applied. Do NOT execute. Do NOT run supabase db push. | none |
| - | Activity inbox read-state + digest (frontend; no schema, no new deps). The Activity surface reads its own inbox_entries with a plain RLS-scoped PostgREST select (50/page, `before` cursor on created_at, "Load more" appends and re-enriches), never a read RPC. The live writer emits minimal payloads (comment/mention = { comment_id }, stage_change = { from, to } with entity_id = post id), so actor display name and entity title are resolved by batched best-effort joins, no N+1: wave one reads comments(id, author_user_id) + posts(id, title) + briefs(id, title) by id, wave two reads users by id; a failed sub-query degrades that field to null instead of failing the feed. Digest threads post/brief entries that share an entity into one group (entityKey) with a "+N more" inline expander, opening the lead marks the whole thread read; solo entries render a single row. Read-state, mark-all, and snooze/unsnooze are optimistic-then-revert through the @srtdio/rpc wrappers (each carries p_trace_id via newTrace()); kebab menu items are >=44px on every breakpoint. Colocated data + row tests (a hand-rolled fake PostgREST client asserts the joins and their degradation, threading order, idempotent mark_read) auto-run by the existing vitest glob. Token-backed classes only, light/dark parity, 44px touch targets. | 38, 16 |
| - | Activity layout: single-line chips + contained thread cards (presentation only; no data, schema, RPC or new deps). Matches the locked Digest prototype. The filter bar collapses the two flex-wrap chip rows into ONE horizontally scrollable line (overflow-x-auto, flex-nowrap, each Chip flex-none + whitespace-nowrap via the additive Chip className passthrough) with a thin 1px border-token vertical divider between the state group (All / Unread / Snoozed) and the scope group (Everything / Posts / Briefs / People / Groups / Clients); it scrolls sideways on narrow viewports and never wraps. Each digest group renders as a single contained card (new src/components/pages/activity/ActivityCard.tsx replacing ActivityRow): bordered panel-bg rounded shell, the entity title shown ONCE as the header, the lead event's short line beneath it (new title-less, actor-less shortLine helper: "Moved to {stage}", "New comment", "New mention", etc.), timestamp, a Posts/Briefs scope tag, and the kebab; a "+N more" toggle expands the remaining events INSIDE the same card (indented, each its own short line + timestamp + unread dot), collapsed by default; a solo group is the same shell with no toggle. Unread group keeps the accent-line border + accent dot by the title. Lead visual: resolved actorName -> Avatar, else the lead event's icon in a tone-tinted circle (approved/brief_closed good, rejected bad, review/parked/trial warn, mention accent, else neutral). Card-body click still marks every entry read (optimistic) and navigates from the lead; the kebab still stops propagation and acts on the group. Bucket headers (Today / Yesterday / Earlier) and the vertical feed are unchanged; only the chips scroll horizontally. data.ts fetch / enrichment / groupDigest and every RPC call are untouched (additive cardTitle + shortLine helpers only). Tests updated to the card structure (ActivityCard.test.tsx: single shell, title shown once, "+N more" collapsed, solo card, unread treatment, tone circle) plus cardTitle/shortLine cases in data.test.ts; the behavioural data tests are unchanged. Token-backed classes only (no new color literals), light/dark parity, 44px touch targets. | 38, 16 |
| - | Activity filter chip bar fix (UI only; no data, schema, RPC or new deps). Extracts the chip row into src/components/pages/activity/ActivityFilterBar.tsx. Removes the 1px vertical divider between the state group and the scope group. Scope chips become single-select toggles (at most one active): tapping a scope selects it, tapping the active scope clears back to the internal default 'everything' (no 'everything' chip rendered); state chips (All / Unread / Snoozed) keep always-one single-select. Pure nextScope(current, clicked) helper drives the toggle. The row is full-bleed to the screen's right edge (negative right margin cancels the page gutter, paddingRight 0) so the boundary chip is clipped by the edge, with an edge-cut fade applied only on overflow: WebkitMaskImage/maskImage = linear-gradient(to right, #000 0, #000 calc(100% - 26px), transparent 100%), measured via a ref (scrollWidth - clientWidth > 2) and a ResizeObserver re-measuring on mount/resize (disconnected on cleanup), 'none' when not overflowing; the mask is on the scroll element itself (no overlay), alpha-based so it is colour-agnostic in light and dark. Scope consumption/filtering downstream is untouched. Colocated ActivityFilterBar.test.tsx covers the nextScope toggle (select, clear, switch). Token-backed classes only, light/dark parity, 44px touch targets preserved. | 38, 16 |
| - | New Post create sheet rebuilt to the locked create-sheet design plus three shared UI building blocks (frontend only; no schema, proc, RPC or generated-types change). New src/components/ui/Select.tsx (compact single-choice dropdown: h-11 truncating trigger with chevron, menu that flips upward near the viewport bottom else downward via a pure computeMenuPlacement, >=44px option rows with an optional left slot for a bucket dot / owner Avatar and a check tick, closes on select / Escape / outside click, controlled, presentational) and src/components/ui/DatePicker.tsx (custom in-app calendar replacing the overflowing native date input: h-11 calendar-icon trigger opening a month-grid popover with prev/next month, day cells, today marker and accent-filled selected day; controlled ISO yyyy-mm-dd; pure toISO/parseISO/shiftMonth/monthGrid/dayCells). New src/components/pages/create/AttachmentsField.tsx replaces the dead dropzone with Photo / Document / Link add actions: Photo + Document reuse the comment composer upload path verbatim (precheckFile / precheckImage + UPLOAD_ACCEPT / IMAGE_ACCEPT + uploadChatAttachment returning an asset VERSION id) and Link reuses addAssetLink then resolves the new asset's current_version_id with a plain RLS-scoped select, items render as removable image tiles / file+link rows with a per-item uploading state, the parent owns the ordered completed list via value + onChange. CreatePostSheet keeps its props (open, workspaceId, onClose, onCreated) and reset-on-open, with required Title + Caption then an always-2-per-row grid (every cell min-w-0, Select labels truncate, body overflow-x-hidden) of Platform (required; labelled "Platform" per the locked prototype although the rest of the app says "Channel"), Format, Bucket (colored dot, optional), Owner (Avatar, defaults to the current user, reusing usePostMembers), Target date (DatePicker) and Origin (None / From a brief revealing a brief Select over the workspace's listBriefs); on submit it validates with PostCreatePayloadSchema (brief_id required when origin is brief), sends the snake_case post_create payload (owner_user_id, bucket_id, origin + brief_id, target_date) via postCreate, then binds any completed attachments in order via gallerySet ({ p_post_id, p_asset_version_ids, p_trace_id }) only when attachments exist. Colocated SSR-safe tests (placement, option rows + dismiss, date math + day pick, file/link upload runners + list ordering/remove, payload build + brief_id requirement + submit gallery wiring + reset-on-open). Token-backed classes only, light/dark parity, 44px touch targets. | 34, 11, 15 |
| - | New Brief create sheet rebuilt to the locked create-sheet design, reusing the shared building blocks (frontend only; no schema, proc, RPC or generated-types change). CreateBriefSheet keeps its props (open, workspaceId, onClose, onCreated) and reset-on-open and the "Briefs are written by clients and are read-only once created." helper line, with required Title (Input) + Objective (Textarea) then the shared AttachmentsField (replacing the old single Reference link Input), a 2-per-row grid (every cell min-w-0, body overflow-x-hidden) of Format requested (the shared Select: an "Any" option that omits format_requested followed by POST_FORMATS with 'link' removed, humanized) and Target date (the shared DatePicker), then an optional Initial comment (Textarea) that seeds the brief's discussion thread; the Brand requirements field is removed. On submit it validates title + objective non-empty, builds the snake_case brief_create payload (title, objective, format_requested only for a real format, target_date when set, attachment_asset_version_ids from the AttachmentsField when non-empty, never brand_requirements or reference_links, no actor id), calls briefCreate as the success gate, then best-effort seeds the thread via createComment (entity_type 'brief', the returned brief id) only when the comment is non-empty, calling onCreated regardless of the seed's outcome. Folds in a 44px tap-target fix: the AttachmentsField image-thumbnail remove button grows from h-7 w-7 to h-11 w-11. Colocated SSR-safe tests (payload build incl. Any omission + no brand_requirements/reference_links, submit gating + best-effort comment seed + brief_create failure, reset-on-open clears every field). Token-backed classes only, light/dark parity, 44px touch targets. | 34, 14, 15 |
| - | Catch-up email core: deterministic summary, render, deep-link URLs, send-window. Adds the PURE, unit-tested rendering layer of the 30-minute catch-up email to @srtdio/inbox (new packages/inbox/src/catchup.ts + catchup.test.ts, exports added to packages/inbox/src/index.ts). No database, no network, no Worker, no cron: names/titles/urls arrive already resolved and `now` is passed in. Types CatchupRow/ChatRow/CatchupData; buildCatchupUrls(base, ws) emitting the post/brief/comment/asset/chat/activity deep links (base never hardcoded, reused by the later Worker); summarizeCatchup() (no AI, numbers spelled to nine: single-item names it, brief gets "opened a brief:" phrasing, multi-item attributes the gist to the dominant actor, appends an unread-chat sentence, falls back to "{N} updates while you were away."); shouldShowCounts() (>=3 distinct non-empty sections) + countCells() (Stage, Comments, Brief, Chat, Asset, capped at 4); isInSendWindow(tz, now) (local Mon-Fri and 10<=hour<18 via Intl, non-UTC verified); renderCatchupEmail() returning subject/html/text/messageId, the html an email-client-safe table layout with inline LIGHT tokens plus a prefers-color-scheme dark @media block and [data-ogsc] Outlook equivalents (light/dark parity), Inter + JetBrains Mono via Google Fonts, the whole row an <a href=row.url>, counts strip only when shouldShowCounts. Boundary-focused tests run under the existing @srtdio/inbox suite; no new workflow. The cron Worker (reads data, sends, stamps) and the frontend deep-link layer are SEPARATE later PRs and intentionally absent. Strict TS, no any. No schema, no migration, no RPC, no new dep; no other package or root file touched. | none |
| - | Catch-up email cron Worker: read activity, gate, send, stamp. New src/server/cron/catchup-send.ts (+ test) on the idempotency-cleanup pattern (exported testable logic + `export default { scheduled }`), wired by adding only the "*/30 * * * *" line to wrangler.workers.toml. runCatchup is pure of direct I/O behind an injected CatchupGateways boundary (fakes in tests); handleScheduled wires a service-role Supabase client (no explicit txn) + the reused createResendSender from @srtdio/workspace and resolves recipient emails via auth.admin.listUsers. Reads inbox_entries (email_sent_at null, not deleted, not snoozed, the eight catch-up event types) in <=1000 batches, groups by (user_id, workspace_id) into one candidate email each, and resolves every actor/recipient/post/brief/asset/peer in ONE batched IN read per kind (no N+1). Per recipient: timezone = users.timezone ?? workspaces.timezone, gated by isInSendWindow + 5-min inactivity over session_devices; assembles rows (comment/mention/decision actor-led, stage_change + brief_closed actor-less, brief_created/asset_uploaded/asset_version_added) and DM counts (chat_channels + chat_messages since the per-recipient last-catchup floor) via the pure @srtdio/inbox core; renders + sends, then stamps email_sent_at in one bulk update and records a delivery_attempts row (template_key 'catchup') mirroring invite.ts. Send-then-stamp: a mid-flight crash risks at most one duplicate next slot. Extends packages/inbox/src/catchup.ts minimally so actor-less rows render without an avatar and are ignored by the dominant-actor summary. Strict TS, no any. No edge function, no schema, no migration, no RPC, no deploy wiring beyond the cron line. | Schema: add users.timezone for catch-up scheduling; Catch-up email core |
| - | Auth session lifecycle fix (frontend + auth server; no schema, no migration, no type regen): two scoped corrections plus regression tests. (A) src/server/auth/session.ts signOut now calls GoTrue logout with { scope: 'local' } instead of 'global', so a user-initiated logout revokes only the current device's refresh token and leaves the user's other devices signed in (confirmed the single ordinary logout path, not a sign-out-all-devices feature). (B) src/lib/session-context.tsx onAuthStateChange no longer blindly clears state on a null session: a new exported pure resolveSession(event, nextSession, current) reducer clears on an explicit SIGNED_OUT, adopts any truthy session, treats a null INITIAL_SESSION as genuinely logged out at boot, and otherwise leaves the current session in place so a transient null from a refresh hiccup (e.g. TOKEN_REFRESHED) lets auto-refresh recover without flashing /signin; the getSession-on-mount block and RouteGuards are untouched. Colocated regression tests: session.signout.test.ts mocks the supabase client and asserts signOut passes { scope: 'local' }; session-context.test.ts drives resolveSession through a captured-callback harness asserting TOKEN_REFRESHED+null keeps the live session, SIGNED_OUT+null clears it, and INITIAL_SESSION+null from empty stays null. Both run under the existing CI `test` job (pnpm test / vitest over src/**/*.test.ts(x)); no new workflow. Strict TS, no any, no @ts-ignore, no .js/.jsx. | 6, 31 |
| - | Email deep-link layer (frontend only; landed): the SPA now honors the catch-up email query params so each click lands on its exact target, every param optional/best-effort, each fired exactly once via a ref guard, each consumer stripping ONLY its own key (next built from the live searchParams, key deleted, setSearchParams replace) while preserving the others. AppLayout reads ?w={workspaceId} and, after loading, sets the active workspace when the id is a member and not already active, gating the Outlet behind the existing-style placeholder while the switch is pending so the entity never mounts under the wrong tenant, then strips 'w'. ?comment={commentId} on /posts/:postId and /briefs/:briefId is captured into state (survives the strip) and passed as the new optional Comments focusCommentId prop; new src/components/comments/flash-node.ts holds flashNode (extracted verbatim from PostDetailPage's flashTo, which now delegates to it), and Comments scrolls-to + flashes the row once it is present in the loaded list. ?channel={channelId} on /chat selects that channel inside ChatConnected's existing initial channel-list effect (read via refs so its deps are unchanged, no extra refetch). ?asset={assetId} on /assets opens that asset in the lightbox via the existing openAsset once the list has loaded, no-op when not in the visible set. No backend, no schema, no router/route-table change; the only new string literals are the param keys 'w'/'comment'/'channel'/'asset'. Colocated flash-node.test.ts (node env + faked DOM globals, no jsdom dep) covers the absent-node no-op and the ring add/remove around the 1200ms timeout. Strict TS, no any. | Catch-up email core; Catch-up email cron Worker |
| - | Deep-link through sign-in: a logged-out user who clicks a catch-up email deep link now lands on the link's target after login instead of being dumped on /pipeline. RequireAuth (src/components/auth/RouteGuards.tsx) captures the attempted location via useLocation and redirects to /signin with the attempted pathname+search preserved in Navigate state ({ from }); SignInPage (src/components/auth/SignInPage.tsx) reads location.state.from (narrowed without any: a string starting with a single '/'), falling back to /pipeline, and navigates there on a successful sign-in instead of the hardcoded /pipeline. Already-authenticated users are unaffected (RequireAuth renders the target route directly). RequireGuest, the loading branch, SignUpPage, the App.tsx route table and all server/schema code untouched; the only literals are the existing route paths and the 'from' state key. Frontend only, no schema, no migration, no RPC, no new dep. | Email deep-link layer |
| - | Invite edge functions made self-contained for the Deno edge bundler: invite-send and invite-accept dropped every @srtdio/* import (the packages' internal relative imports omit the .ts extension, which the Supabase edge bundler rejects), so each function index.ts now inlines its equivalent logic and each deno.json maps only @supabase/supabase-js + zod. invite-send inlines inviteMember (resolve/provision the invitee via the admin API + generateLink type=invite, dedup a pending workspace_members row via the caller client, member_invite as the caller so auth.uid() is the inviter, one delivery_attempts row, Resend via fetch not the npm SDK) and the invite email subject/body; invite-accept inlines acceptInvite (member_accept as the caller). Proc names/params (member_invite p_workspace_id/p_email/p_role/p_trace_id, member_accept p_invite_id/p_trace_id), role values (admin/agency/client), and the email subject/body are preserved verbatim. trace_id read from X-Trace-Id else a uuid v7 generated inline, threaded to the RPC and echoed on responses; CORS allow-origin from APP_BASE_URL with OPTIONS handled. Device-fingerprint middleware is deliberately omitted from both endpoints (deferred to post-cutover hardening); verify_jwt stays ON. No package, schema, migration, type-regen, or workflow change. | 7, 8 |
| - | Invite backend deployability: make invite-send and invite-accept the canonical, deployable invite edge functions and remove the accidental member-invite duplicate (withdrawn prompt). Adds a per-function deno.json import map to each (mapping @supabase/supabase-js + zod to npm and @srtdio/auth, @srtdio/workspace, @srtdio/rpc, @srtdio/schemas to their packages/<name>/src relative entrypoints, consistent with the session-register precedent) so Deno resolves the @srtdio/* bare imports the function index.ts already uses. Adds a deploy-edge-functions GitHub Actions workflow (push to main on supabase/functions/** + workflow_dispatch) that uses supabase/setup-cli, authenticates via the SUPABASE_ACCESS_TOKEN secret, reads the project ref from the SUPABASE_PROJECT_REF Actions Variable (never hardcoded), and deploys ONLY invite-send and invite-accept with JWT verification left on (no --no-verify-jwt); the other functions still lack a deno.json and are not touched. Deletes supabase/functions/member-invite/ and its Edge Invite CI workflow. Backend infra only: no function index.ts logic change, no package change, no schema, no migration, no type regen. | 7, 8 |
| - | Invite-send provisioning + failure visibility fix (edge only; no schema, no frontend, no package): invite-send now provisions an absent invitee with adminClient.auth.admin.createUser({ email, email_confirm: true }) (a confirmed user, no provider email) instead of generateLink type=invite, keeping the find-first reuse so an existing user is never recreated; on createUser failure it console.errors the real error with the trace id and returns { error: 'provisioning_failed', error_detail, trace_id } (500). After member_invite yields the invite id it mints a passwordless adminClient.auth.admin.generateLink({ type: 'magiclink', email, options: { redirectTo: `${appBaseUrl}/invite/set-password?invite=${inviteId}` } }) and emails properties.action_link via Resend (replacing the old /invite/accept link in the body); a generateLink failure does NOT fail the request: the created user and invite row survive, emailed=false, the error text is written to the delivery_attempts row and the response error_detail, and console.errored with the trace id. Every silent { error: 'unknown' } across both functions is replaced with console.error(traceId, message) + a specific code (provisioning_failed / delivery_record_failed / internal, or the member_invite/member_accept domain code for mapped statuses) plus error_detail, keeping the existing HTTP status; a bare 'unknown' is never returned and no secret is ever logged or echoed. Missing RESEND_API_KEY or INVITE_FROM_ADDRESS sets emailed=false with error_detail 'resend_not_configured' and a console.error, never a throw. invite-accept gets the same console.error + { error: code, error_detail, trace_id } treatment with no other behaviour change. Trace-id handling, CORS, the caller-client member_invite call (auth.uid() is the inviter), the dedup query, and the delivery_attempts insert/update are unchanged. verify_jwt untouched, no new dependency, deno.json untouched. | 7, 8 |

| - | Accept-invite screen (frontend only; no schema, migration, RPC, package or generated-types change). New /invite/accept route mounted under RequireAuth but OUTSIDE WorkspaceLayout/WorkspaceProvider (an invitee has no workspace yet), added as the first child Route before WorkspaceLayout with nothing else in App.tsx changed. AcceptInvitePage reads the invite id from the `invite` query param via useSearchParams: absent/empty renders an "This invite link is invalid." state with a sign-in button and never calls the function; otherwise a centered AuthShell card (heading + one body line + primary "Accept invitation" button, no workspace name shown) accepts only on a deliberate click. Accept generates a crypto.randomUUID() trace id (outside TraceProvider), calls supabase.functions.invoke('invite-accept', { body: { invite_id }, headers: { 'x-trace-id' } }), navigates to /pipeline (replace) on success, and on error shows a friendly code-free message plus a secondary "Go to your workspace" button to /pipeline so an already-joined user is never stuck. Pure exported acceptInvite helper (invoke + navigate injected) plus the static states are covered by colocated tests under the existing vitest glob (missing param shows the invalid state and never invokes; success invokes with { invite_id } and signals navigation; an invoke error signals the error path). Mirrors SignInPage structure/tokens, light/dark parity, 44px touch targets. | 31 |

| - | Members invite form (frontend only; no schema, migration, RPC, package or generated-types change). Replaces the Settings > Members EmptyState placeholder body (MembersPanel in SettingsPage.tsx) with a new src/components/pages/settings/MembersInviteForm.tsx that calls the already-deployed invite-send edge function from inside the workspace shell. Reads the active workspace via useWorkspace() (renders a "No active workspace." line when null) and a fresh trace per submit via useNewTrace(). Email (Input type=email, trimmed on submit) + Role (shared Select: Admin / Agency (team member) / Client (reviewer), defaulting to client, values matching the function enum) wrapped in Field; a size-lg primary "Send invite" disabled while submitting and until the email is non-empty and contains '@' (light client guard; the function is authoritative). Pure exported sendInvite helper (invoke injected, narrowly typed for plain-mock unit tests) POSTs supabase.functions.invoke('invite-send', { body: { workspace_id, email, role }, headers: { 'x-trace-id' } }) and branches any non-null/undefined error to onError else onSuccess; on success it clears the email (keeps the role) and shows "Invitation sent to {email}.", on error it keeps the email and shows a friendly code-free line, never parsing server codes. Colocated tests (mocked supabase + in-shell hooks) cover the exact success invoke + onSuccess, the error onError, and a render assertion (email field, role default client, Send invite, no status text, invoke not called on mount). Token-backed text-good / text-bad status classes, light/dark parity, 44px touch targets. Out of scope: existing-members list, the other Settings panels, tab wiring. | 8, 31 |

| - | Byte-copy idempotency for the asset migration (ETL + storage; no schema, migration, type-regen, or workflow change). Makes a failed/resumed cutover run retryable with no orphan R2 objects. ROOT CAUSE: packages/etl/src/assets.ts minted a fresh uuidv7 asset id per run and buildR2Key embeds it, so re-runs produced new keys and orphaned the prior objects, with no exists-check before re-uploading. CHANGE: the FILE-path asset id is now deterministic, ref.assetId = uuidv5(normalizeMediaPath(item.url), fixed-namespace) on both the cutover and dev-seed branches, so the r2_key is stable across runs; versionId and attachment id stay uuidv7 (DB-only, reloaded each run) and link assets keep uuidv7 ids (no bytes, no key). The cutover file path now calls a new StorageClient.objectExists(bucket, key, traceId) before uploading: if present it SKIPS putObject but still records the asset/version/attachment rows; correctness on retry relies on the v1 write-freeze making each source URL's bytes immutable during the cutover window. objectExists is implemented on R2StorageClient via a signed S3 HEAD (200 true, 404 false, else throw) and on InMemoryStorageClient via the objects map. Dedup-by-sha, fetch, summary counts, and link handling are otherwise unchanged. Tests under tests/etl + tests/assets cover deterministic key/id across two runs, skip-if-present (pre-seeded InMemoryStorageClient, bytes not re-put while rows recorded), and InMemoryStorageClient.objectExists true/false. | 28 |
| - | ETL cutover rehearsal Step-0 (enabling only; no load logic change, no R2 delete, no schema). Adds a confirm-gated `--wipe` action to @srtdio/etl and a dispatch-only workflow that drives dry-run / wipe / cutover / validate. config.ts: optional `wipe`/`confirmWipe`/`validate` on the Cli interface (optional so the existing load-suite EtlConfig fixtures keep type-checking; parseCli always returns concrete booleans), parseCli learns `--wipe`/`--confirm-wipe`/`--validate` and keeps unknown-arg rejection, assertSafe gains a `--confirm-wipe` gate while the same-database refusal and EXPECTED_TARGET_REF pinning stay in force for a wipe (a wipe can never hit v1), and the confirm-cutover gate now fires ONLY for an actual cutover load (skipped for validate or wipe). index.ts: a new wipe(config) runner builds a TARGET Pool only (no SourceDb, no R2), resolves the workspace read-only by name (fails clearly when absent), then calls the existing wipeWorkspaceContent inside BEGIN/COMMIT mirroring migrate() (ROLLBACK + rethrow on error, client/pool closed in finally); main() branches to it before validate/dryRun/migrate, and a recognised-but-unimplemented validate branch throws rather than ever falling through to a load. New .github/workflows/etl-cutover.yml is workflow_dispatch ONLY (never push/PR), inputs action (choice) + confirm (string); a first guard fails the job unless confirm equals the action word for wipe/cutover; actions map dry-run -> --mode=cutover --dry-run, wipe -> --wipe --confirm-wipe, cutover -> --mode=cutover --confirm-cutover, validate -> --validate, invoked exactly as etl-dev-seed.yml runs the package and passing the same repo secrets it uses (plus CLOUDFLARE_ACCOUNT_ID / CLOUDFLARE_R2_ACCESS_KEY_ID / CLOUDFLARE_R2_SECRET_ACCESS_KEY by reference only). Both config test locations gain parse + guard coverage (wipe/confirmWipe defaults, --wipe/--confirm-wipe parsing, wipe-without-confirm refusal, same-database and target-ref refusal for a wipe, and validate/wipe not needing --confirm-cutover). load.ts/assets.ts/validate logic, source.ts, transform.ts, schema, generated types, workers, edge functions, frontend, root config and etl-dev-seed.yml all untouched. | none |

| - | Pre-flip CORS enablement: add an explicit ALLOWED_ORIGINS Worker var to the asset-read, asset-upload, and chat-token wrangler.toml files so the pages.dev app origin is permitted cross-origin during pre-flip testing alongside srtd.io. Each file gains (or extends) a [vars] table with ALLOWED_ORIGINS = "https://srtd.io,https://srtdio-app.pages.dev"; the workers already read env.ALLOWED_ORIGINS (comma-separated, trimmed) and fall back to a srtd.io-only default when unset, so setting the var explicitly allows both origins. CI-only config change: no .ts, no test, no DEFAULT_ALLOWED_ORIGINS, no secret, no schema, no other worker or workflow touched. Reversed at cutover by the Harden worker CORS PR. | none |
| - | Harden worker CORS: drop the pages.dev test origin from the default allowed origins in asset-read, asset-upload, and chat-token workers, leaving srtd.io as the sole production default when ALLOWED_ORIGINS is unset. Only the DEFAULT_ALLOWED_ORIGINS constant (and its explanatory comment) and the matching worker unit-test expectations change; the ALLOWED_ORIGINS override logic, allowedOrigins()/resolveAllowedOrigin(), and every other line stay untouched. Intended for production cutover. Strict TS, no any. No schema, no migration, no RPC, no package or root-config edits. | none |

| - | asset-upload deploy pushes R2 secrets: the asset-upload deploy workflow now pushes the Worker's R2 credentials on every deploy, mirroring the asset-read deploy workflow, so the presigned R2 PUT URLs sign for the correct Cloudflare account instead of a stale hand-set dev account. The existing wrangler-action Deploy step gains a `secrets:` list (CLOUDFLARE_ACCOUNT_ID, CLOUDFLARE_R2_ACCESS_KEY_ID, CLOUDFLARE_R2_SECRET_ACCESS_KEY) and a matching `env:` block wiring each from ${{ secrets.* }}, the three names identical to asset-read. CI-only config change: no .ts, no wrangler.toml, no other workflow, no secret value printed or hardcoded, no schema. | none |

| - | Client sign-out wiring: the shell dispatches a `sorted:signout` window event but nothing on the client ends the Supabase session, so Sign out never logs the user out. SessionProvider now registers a `SIGNOUT_EVENT` window listener inside its existing effect (removed on cleanup) that calls a new exported `performSignout(client)` helper; the helper calls `client.auth.signOut({ scope: 'local' })` (never other devices, matching src/server/auth/session.ts) and logs via logger on error, letting the existing onAuthStateChange SIGNED_OUT path clear the session and route to /signin (no manual setSession(null)). Unit tests cover the local-scope call and the error path resolving without throwing. No schema, no new package. | none |

| - | Member-invite form surfaces the edge function's real failure reason: sendInvite discarded the invoke error and rendered a single constant (INVITE_ERROR_MESSAGE), hiding why an invite failed. On error it now best-effort reads the FunctionsHttpError's `context` Response body (the function's { error, error_detail, trace_id } shape) via `await context.json()` in a guarded try/catch, narrowed without any/ts-ignore, and builds a user-facing "Invite failed: <error_detail> (trace <trace_id>)" message (trace omitted when absent). The boolean `failed` state becomes a nullable `errorMessage` threaded through the existing onError callback and rendered in the same `text-sm text-bad` element; when no JSON body with a detail can be read it falls back to INVITE_ERROR_MESSAGE. Success path, request body, role select, client, and auth handling untouched. Light + dark parity via the existing error style, 44x44 targets unchanged. Frontend-only, no schema, no edge function change. | 8 |
| - | Regenerate Supabase types from live database: workflow_dispatch-only GitHub Actions workflow (.github/workflows/types-regen.yml) that regenerates packages/schemas/src/supabase.generated.ts from the live v2 public schema and opens a PR with the diff. Mirrors schema-drift.yml's env setup (checkout@v4, pnpm/action-setup@v4, setup-node@v4 node 22 + pnpm cache, frozen-lockfile install) then runs pnpm types:supabase with SUPABASE_ACCESS_TOKEN + SUPABASE_PROJECT_ID from secrets (project id never hardcoded), and peter-evans/create-pull-request@v6 opens a PR scoped via add-paths to the generated types file alone (branch chore/regen-supabase-types, delete-branch, base main). No diff is a clean no-op that opens no PR. The reusable unblock for the types-refresh problem; concurrency group types-regen, cancel-in-progress false. Never fires on push/PR. Touches no package source. Status: in-progress. | none |

| - | New avatar-upload Worker: public per-user profile-photo storage. POST multipart/form-data { file } gated by the caller's Supabase JWT (ES256 JWKS verify, reusing asset-read's verifyCaller/getSupabaseJwks); the actor is the verified token `sub` only, never the body. Deliberately simpler than asset-upload: an avatar belongs to a PERSON not a workspace, so there is NO workspace_id, NO membership check, NO database access, and NO service-role key. Validates PNG-only (magic-byte checked; SVG and all other types rejected 415), empty -> 400, > 5 MiB -> 413, then PUTs to the pre-provisioned public R2 bucket `user-avatars` (env AVATAR_BUCKET) at key `${userId}/${sha256}.png` (hash busts CDN caches on photo change; userId folder isolates each person and constrains a caller to their own avatar) and returns 200 { avatar_url } on cdn.srtd.io (env AVATAR_PUBLIC_BASE), the URL path equal to the R2 key. No EXIF/virus scan (PNG-only, size-capped, self-produced by the in-app cropper canvas). R2StorageClient.putObject + sha256/magic-byte helpers reused from @srtdio/storage; trace-id propagated, CORS mirrored from asset-upload (allowlisted origins, never `*`), OPTIONS -> 204, other verbs -> 405. The frontend (later) takes avatar_url and calls user_profile_update; this worker never touches the users table. New wrangler.toml (name srtdio-avatar-upload), deploy workflow (3 R2 secrets only, no Supabase secret), and unit tests; shared vitest glob runs the new test. Serves the onboarding profile photo. Backend-only, no UI, no schema, no new package. | none |
| - | Frontend fix: show the user's avatar photo on comment authors (Comments.tsx resolves avatar_url from the existing commentProfiles map and spreads it as the Avatar `src`) and on the app header's top-right profile button (AppLayout forwards the already-fetched useCurrentProfile display_name + avatar_url to Topbar, which spreads them onto its Avatar), mirroring the PostDetailPage owner-byline spread idiom (src only when non-null, exactOptionalPropertyTypes-safe; initials/generic-icon fallback preserved). No schema, no new fetch, no new package. | none |
| - | Frontend fix: attribute migrated v1 comments to their original author in the comment panel (Comments.tsx only). Migrated rows carry comment.legacy_author_name (and legacy_author_email) but the card rendered the FK author (the cutover operator), so every migrated comment showed the operator. The card now prefers comment.legacy_author_name ?? nameOf(author_user_id) ?? "(ex-member)" for the name, returns null for the avatar src when legacy_author_name is set (initials from the legacy name, never the operator's photo), canModifyComment returns false for any legacy comment so edit/delete never appear even when the current user equals the stored author_user_id, and tombstoneText prefers the legacy name. Mention rendering, the comments query/data layer, and all styling classes are untouched; the only visible change is the name string and an initials avatar, light/dark parity preserved. Colocated tests cover the legacy-name preference, the no-src avatar, canModifyComment false for a legacy comment, and the tombstone legacy name. No schema, no migration, no RPC, no package or data-layer edits. | 13, 28 |
| - | Frontend fix: resolve the brief's "Created by" to a current member's display name on the brief detail page (display only). BriefDetailPage rendered brief.legacy_author_name ?? brief.created_by ?? "(ex-member)", so a brief created by a current member showed a raw uuid. A new exported pure helper briefAuthorLabel(createdBy, legacyAuthorName, memberName) prefers the resolved current-member name, then the ETL legacy_author_name, then "(ex-member)"; the page calls the existing usePostMembers(workspaceId) hook unmodified, builds a useMemo userId to displayName Map alongside the other top-level hooks (before any early return), and the "Created by" dd uses briefAuthorLabel(brief.created_by, brief.legacy_author_name, (id) => memberById.get(id) ?? null). Surrounding dd/className unchanged, light/dark parity preserved. Colocated tests cover every branch (member resolves and wins over a present legacy name, unresolved falls back to the legacy name, null created_by returns the legacy name, unresolved/null with no legacy name returns "(ex-member)"). No reads/data-layer, schema, migration, RPC, the member hook, or package edits. | 13 |
| - | Frontend fix: show migrated v1 author names on the post and brief detail surfaces (display only). posts, briefs, and post_versions carry legacy_author_name from the cutover, but the detail pages resolved only the live FK author and fell back to "(ex-member)", hiding the filled names. PostVersionView now carries legacyAuthorName (populated in toVersionViews off the embedded post_versions row); PostDetailPage's resolveOwner and resolveAuthorName take the legacy name as a second argument and fall back to it before "Unassigned"/"(ex-member)", a resolved current member still winning; VersionHistorySheet's resolveAuthor prop takes (userId, legacyName) and the call site threads version.legacyAuthorName; BriefDetailPage's "Created by" prefers brief.legacy_author_name ?? brief.created_by ?? "(ex-member)". Reuses existing text/avatar classes only, light/dark parity preserved. Colocated tests cover the legacy-name fallback and the current-member-preferred path. No reads/data-layer, schema, migration, RPC, or package edits. | 13 |
| - | Migration parity: record the posts, briefs, and post_versions legacy_author_name columns (plain nullable text, no constraints) as a migration. The columns were added out-of-band during the cutover and are already live and present in the committed generated types, but no migration recorded them; this records them for parity. legacy_author_name preserves the original v1 creator/editor display name on migrated rows whose *_by author was set NULL at cutover, rendered as the author with an "(ex-member)" fallback. Recording only: the migration header notes it was already applied via MCP and must not re-run, schema.md notes the column on all three tables, no DB execution, no type changes, comments legacy columns untouched (recorded separately). | 13 |
| - | Schema + types: post.delete capability (owner/admin; backfilled to all existing workspaces and added to the seed_workspace_role_defaults trigger for new ones) and a post_soft_delete(p_post_id, p_trace_id) SECURITY DEFINER proc that sets posts.deleted_at and bumps row_version, gated on proc_capability('post.delete'), EXECUTE revoked from PUBLIC and granted to authenticated. Schema already applied via MCP; migration recorded. supabase.generated.ts adds only the post_soft_delete Functions entry. No RPC wrapper, no UI. | none |
| - | Desktop-style auto-numbering for folder CREATE on the asset-upload Worker. createFolder resolves a sibling-name collision server-side (race-safe, in the repository) instead of returning 409: it reads the active sibling names for the (workspace_id, parent_id) in one query (NULL parent grouped, case-insensitive against the folders_unique_name lower(name) shape), keeps the trimmed typed name as BASE when free else picks the lowest N>=2 for "BASE N" ("Campaigns 2", "Campaigns 3", ...), truncates the base so the final name stays <=80 (folders.name limit), then inserts; a 23505 from a concurrent creator retries from the read, bounded at 5 attempts before throwing (500). createFolder's return type drops the Result wrapper (Promise<FolderSummary>); handleCreateFolder loses its 409 branch and returns 201 { folder } with the resolved name, keeping all input validation, the membership gate, and the cross-workspace parent check. RENAME, DELETE, and MOVE are UNCHANGED; renameFolder still returns Result and still 409 folder_name_taken on a sibling collision (FolderError/FolderErrorCode retained). InMemoryAssetRepository mirrors the same per-parent base/suffix/truncation logic. Tests updated in place (no new file): duplicate create now yields "Campaigns 2"/"Campaigns 3", case-insensitive "campaigns" -> "campaigns 2", same base under two parents stays bare; all existing folder/membership/role tests (including the rename 409) kept. No schema, SQL, migration, generated types, or frontend. | none |
| - | Wire the Assets screen to the real folders table (browse + create). Frontend only; the folders table, assets.folder_id, and the worker /folders create route are already live. Navigation moves off the legacy folder_path string onto real folder rows (folder_id, null = root); existing assets keep folder_id NULL so they all sit unchanged at the root. assets.ts adds AssetListItem.folderId (and RawAssetRow.folder_id/ASSET_SELECT/shapeAssets mapping; folder_path left populated, its column drop deferred to a later cleanup PR), FolderItem + fetchFolders (RLS-scoped SELECT id,name,parent_id), childFolders (parent filter, caseless name sort), folderBreadcrumb (root->current, cycle-guarded, [] for null/unknown), folderChildCount (child folders + assets in a folder), and removes the folder_path subfolders/breadcrumbSegments helpers. asset-upload.ts adds createFolderRequest (POST /folders {workspace_id,name,parent_id}, Bearer, injected fetcher, never-throws FolderCreateOutcome, folderErrorMessage generic-retry map; create never returns folder_name_taken). New FolderCard (AssetCard container, IconFolder cover, "N items", whole-card open, no long-press/menu) and NewFolderSheet (AddLinkSheet mirror; 1..80 name gate, toasts the server-resolved possibly-auto-numbered name, refresh+close). AssetAddMenu gains a first "New folder" item. AssetsPage swaps folder-string state for folderId, loads folders alongside assets, scopes the grid + chip counts by folderId, renders a folder-card grid and real-folder breadcrumb, and adds handleCreateFolder (env+workspace+token guarded). New folder is allowed for every active member. Upload-into-folder and folder rename/delete/move are later PRs. No backend, schema, SQL, migration, or generated types. | none |
| - | Folder rename + delete on the Assets screen via a long-press folder action sheet (owner/admin/agency only; the affordance is hidden for everyone else and the worker enforces it as defense in depth). Frontend only; the worker /folders/rename and /folders/delete routes are already live. asset-upload.ts adds renameFolderRequest (POST /folders/rename {workspace_id,folder_id,name}, never-throws FolderRenameOutcome surfacing folder_name_taken as nameTaken:true, a 403 agency-only line, else generic retry) and deleteFolderRequest (POST /folders/delete {workspace_id,folder_id}, never-throws FolderDeleteOutcome, 403 agency-only line, else generic retry). FolderCard gains an optional onManage that wires useLongPress (long-press opens the action sheet; a tap still opens; with no manage rights a long-press just opens). New FolderActionSheet (AssetActionSheet container/Row mirror; Rename + destructive Delete rows, Delete swaps to an in-sheet confirm "Files inside move to All assets") and FolderRenameSheet (NewFolderSheet mirror; prefilled name, Save disabled until the trimmed name changes and is 1..80, surfaces folder_name_taken inline, toasts the server-resolved name on success). AssetsPage adds canManageFolders (= canRename), folderAction/renameTarget state (reset on workspace change), passes onManage to FolderCard, and handleRenameFolder (reloads folders on success) / handleDeleteFolder (reloads folders AND assets, since the worker detaches the folder's contents to the root). No backend, schema, SQL, migration, or generated types. | none |
| - | Select files on the Assets screen and move them into a folder (or back to the root). Frontend only; the worker /folders/move {workspace_id, asset_ids[], target_folder_id} route is already live and allowed for every active member (batch-updates folder_id, returns { moved }). assets.ts adds flattenFolders (depth-first walk from the root, each folder once with its depth, siblings name-sorted via childFolders) powering the destination list. asset-upload.ts adds moveAssetsRequest (POST /folders/move {workspace_id,asset_ids,target_folder_id}, Bearer, injected fetcher, never-throws MoveAssetsOutcome reading a numeric moved count, generic retry copy on a non-OK status or thrown fetcher; no role gate). AssetCard gains selectMode/selected/onToggleSelect (a tap toggles selection instead of opening, aria-pressed + "Select {name}", an accent outline and a top-right tick overlay); AssetGrid passes the selection props through. AssetAddMenu gains an optional onSelect that renders a final "Select files to move" item (IconCheck). New MoveToFolderSheet (Sheet of an "All assets" root row + the flattened folders indented by depth; picking a destination runs the pure submitMove, which on success toasts the moved count, refreshes, and closes, else toasts the error and stays open; rows disable while a move is in flight). AssetsPage adds selectMode/selected/moveOpen state (reset on workspace change), shows the "Select files to move" option only when the open folder has assets, swaps the kind chips for a "{n} selected" + Cancel/Move toolbar while selecting (hiding the folder cards, keeping the breadcrumb), caps the selection at 200 with a toast, and adds handleMoveAssets (env/workspace/token guarded) wired to the sheet, which reloads assets + folders and exits select mode on success. No backend, schema, SQL, migration, or generated types. | none |
| - | Assets: upload into a folder with auto-naming; align rename to the display-name field. Backend-only on the asset-upload Worker; assets.filename / display_name / folder_id are already live (no schema, no generated-types change). Naming model enforced throughout: filename is the immutable original upload name WITH its extension (read by the type-icon/preview logic, never overwritten); display_name is the friendly shown name (displayLabel resolves display_name ?? filename). handlePost reads two new OPTIONAL multipart fields, folder_id and display_name: a present folder_id must be a UUID and a live folder in this workspace (repository.getFolder, else 400 bad_request "Folder not found in this workspace." - the cross-tenant guard), a present display_name is validated with validName (1..500, else 400); both thread authorizeAndUpload -> runUploadPipeline. Before insertAsset the pipeline resolves display_name by precedence: a supplied display_name, else (folder upload) a new repository.nextFolderUploadName(workspaceId, folderId), else null; filename always stays the original. nextFolderUploadName returns "${folderName} ${max+1}" over the folder's asset display_names matching ^<folderName> (\d+)$ case-insensitively (folder name regex-escaped), or "${folderName} 1" when none match - monotonic max+1, never count-based, soft-deleted rows included so a freed number is never reused; no lock/transaction/retry since display_name has no uniqueness constraint (a concurrent duplicate number is an accepted cosmetic dup). insertAsset (NewAsset) gains optional folderId + displayName (default null); link create is unchanged (folder_id null, display_name null). Rename bug fix: repository.renameAsset now UPDATEs display_name (not filename), so renaming a non-image no longer strips its type glyph; pipeline renameAsset reads the prior display_name, audits { display_name, previous_display_name }, and returns the summary with the original filename plus the new displayName (AssetMetaSummary gains displayName). InMemoryAssetRepository mirrors every method. Tests extended in place (pipeline + worker suites): folder auto-naming "<folder> 1"/"<folder> 2" then "<folder> 3" after deleting "<folder> 2", root upload with/without display_name, cross-tenant folder_id 400, rename writes display_name and preserves filename, and an auto-named asset's displayLabel changing on rename. No schema, migration, generated types, or frontend. | none |
| - | Assets: upload into a folder (auto-named), single/bulk naming at root, original filename preserved. Frontend only; depends on the merged Worker PR that accepts optional multipart folder_id + display_name and stores filename = original, display_name = the shown label (displayLabel resolves display_name ?? filename). Every upload now sends the file under its ORIGINAL device name (filename keeps its extension for type icons) and the friendly name rides in display_name; the typed/auto name is never the file part. asset-upload.ts: uploadAssetFile appends the file under config.filename (the original), adds display_name only when a non-empty value is given and folder_id only when a folderId is given; UploadConfig gains displayName?/folderId? and the stale "no rename path / follow-up" note is removed; new pure nextUploadNames(base, existingLabels, count) returns "${base} max+1".."${base} max+count" where max is the largest N among labels matching ^<base> (\d+)$ case-insensitively with base regex-escaped (0 when none), incrementing over existing labels not the count; obsolete uploadFilename and its test removed. AssetUploadSheet.tsx derives a mode from scope + count: inside a folder no name input shows and an "Auto-named: <folder> 1, <folder> 2, ..." preview reflects the count; at the root a single file takes one required name (seeded from the filename) used verbatim, two or more share one required base shown as a live "<base> 1, <base> 2, ..." preview; on commit each display_name is computed positionally via nextUploadNames (folder/bulk) or the typed name (single), capped at 500 by trimming the base so the numeric suffix survives, and uploaded sequentially via the existing runUploads path sending original filename + display_name + folderId; dedup (reused=true) handling is unchanged. AssetsPage.tsx passes folderId, the current folder's name, and the current scope's displayLabels as siblingLabels, and wires onSubmit to uploadAssetFile with filename = file.name. New props are optional (root defaults) so the post-detail reuse of the sheet is unaffected. PostDetailPage.tsx's upload handler is migrated to the new onSubmit signature (file, displayName, folderId): it sends filename = file.name (the original device name) and forwards display_name and folder_id, so the original name + extension are preserved and the friendly name is no longer dropped into the filename slot; the sheet stays in root mode there so folderId arrives null (post attachments are not filed into a folder). The now-dead allNamed helper and its test are removed. Light + dark + 44px touch verified. New colocated AssetUploadSheet.test.tsx covers the folder/root-single/root-bulk naming and that the file part keeps the original name; asset-upload.test.ts covers the file-name part, display_name/folder_id inclusion/omission, and nextUploadNames numbering. No backend, schema, migration, generated types, or worker change. | none |
| - | Persist the Agora chat connection at the workspace shell instead of mounting it per chat-page visit. ChatProvider was mounted inside ChatPage (so the connection reopened on every /chat visit and tore down on navigate-away) and again inside PostActionSheet (a second connection). Lift the single ChatProvider into WorkspaceLayout (App.tsx), nested inside SessionProvider and WorkspaceProvider (its connection hook reads useSession() and useWorkspace()) and wrapping AppLayout, so every in-shell page shares one warm connection that opens once per session and persists across route changes, tearing down only on signout or workspace switch via the connection hook's existing cleanup. ChatPage now renders ChatScreen directly (consuming the shared connection via the existing useChat) and drops its local ChatProvider wrapper/import and stale page-scoped comments; PostActionSheet's ConversationPicker consumes the same shell connection and drops its nested ChatProvider wrapper/import. Safe with no added guards because the connection hook/controller already resolve every expected failure to status 'unavailable' and never throw, so a chat-down state cannot break other shell pages. No chat logic, hook, controller, connection, token, RPC, worker, schema, or generated-types change; no visual, styling, or theme change. Frontend only. | none |
| - | PCS polish (post-detail, comments, actions sheet; frontend only, no schema, no migration, no package add). PostDetailPage header drops the duplicate centred title for a left-aligned stage-dot + relative status line (Updated 3h ago / In review · 2h / Approved Jun 11) from shared pure helpers (src/lib/relative-time.ts relativeShort/relativeLong/shortDate + src/lib/post-detail-presentation.ts platformLabel/formatLabel/postStatusLine, both unit-tested), reusing the existing per-stage colour for the dot; the body title becomes plain heading text with a dedicated boxed 44x44 edit pencil (the title itself no longer tappable); the badge row reads LinkedIn / X / Single Image via the label helpers (dead humanize removed); the owner row drops the From brief / Direct origin tag; an empty agency-side post shows an "Add images" upload zone (IconImagePlus) wired to the existing append flow instead of the small Add button. Comments collapse the inline Copy / Edit / Delete into a per-row MenuPopover kebab (IconMore, 44x44, gating preserved: Copy on any live comment, Edit/Delete author-only) with the author-row time switched to relativeLong and tightened card spacing. The actions sheet swaps IconChat for a paper-plane IconSend and adds an owner/admin-only Delete post row + in-sheet confirm view calling the new @srtdio/rpc postSoftDelete wrapper (p_trace_id from newTrace, navigates to /pipeline on success). Token-backed classes only, light/dark parity, 44x44 touch targets throughout. | none |
| - | Bulk-delete on the Assets select-mode bar, plus a pinned select bar. Frontend only; the asset_delete_many(p_asset_ids[], p_trace_id) SECURITY DEFINER proc and its generated types are already live and merged (no schema, no generated-types change). @srtdio/rpc adds the assetDeleteMany wrapper (mirrors assetDelete: callProc 'asset_delete_many', args { p_asset_ids, p_trace_id } passed through unchanged, never a user id - the actor is auth.uid() server-side) with a unit test asserting the proc name + args forwarding and the domain/unknown error mapping. AssetsPage adds a destructive Delete button (danger/bad token, distinct from the primary Move) to the existing "{n} selected" + Cancel/Move bar, disabled at 0 and above 200 behind the pure canBulkDelete predicate (1..200, so the RPC is never called with 0 or more than the cap; an inline text-fg-3 hint "Select 200 or fewer to delete at once." shows above the cap). Delete opens the EXISTING ConfirmDialog (destructive, "Delete files?", correctly pluralised message, confirmLabel "Delete", busy + busyLabel "Deleting..."); on confirm it optimistically removes all selected rows, makes ONE assetDeleteMany call (atomic: on error rolls back the full removal and toasts, on success clears selection, exits select mode, and toasts the deleted count), mirroring deleteOne's optimistic+rollback shape. SectionHeader gains one opt-in stickyChildren prop (default false, so the other 3 callers render identically) that pins ONLY the children action-bar to the top of the scroll area below the app top bar with a solid bg token, border-b, and a z-index above the grid; the title/search row is never sticky; AssetsPage passes it true only while selectMode. Light + dark + 44px touch verified. No backend, schema, SQL, migration, generated types, or worker change. Status: in-progress. | none |

## 5. Handoff rules

- One PR per Claude Code prompt.
- Every prompt ends with: "Return your entire response in ONE code block, most token-efficient form, no prose outside it." and "After pushing, reply with the exact GitHub PR URL in this format: https://github.com/srtdio/app/pull/[PR-number]".
- Audit prompts before any fix prompt. Report-only: no branches, PRs, version bumps, or CLAUDE.md edits.
- Schema changes: state SQL in chat, get approval, execute via Supabase MCP with project_id="movnexawfhsyuluspxoc" passed explicitly, verify via information_schema, then the PR prompt notes "Schema already applied. Do NOT execute." Never target v1 from a v2 schema change.
- Auto-merge disabled. Shubham merges manually. Never defer or pause a PR.
- TypeScript strict everywhere. No .js, .jsx, .mjs, .cjs anywhere (scripts, configs, tests, workers, edge functions all .ts/.tsx).
- Phase order holds: backend, then ETL, then frontend. No interleaving.
- Critical-surface PRs (auth, RLS, ETL, comments, stage transitions, PCS, post lifecycle, billing, cutover) require E2E tests in scope.
- Internal team (Hinglish Agency) is the beta cohort throughout build.
