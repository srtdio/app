// TODO: Define WorkspaceSchema once the `workspaces` table exists in the
// Supabase `public` schema. Mirror the table row shape with z.object({...}),
// snake_case field names matching the database, then export the inferred type:
// `export type Workspace = z.infer<typeof WorkspaceSchema>`.

export {};
